# 마르코! (MARCO!) — 상세 게임 기획서

> Technical Game Design Document · Claude Code 구현 참조용
> 상위 문서: 「유료 멀티플레이어 게임 아이디어 도출 결과」(2026-07-18) 4단계 선정작 파트
> 이 문서는 위 보고서의 전략적 요약을 실제 구현 가능한 수준으로 구체화한 것이다. 표/수치/상태기계는 모두 MVP(10주) 스코프를 기준으로 하며, v1.x 항목은 별도 표기했다.

---

## 0. 문서 정보

| 항목 | 내용 |
|---|---|
| 버전 | v0.2 (상세화 2차 — 조작 체계·사운드 전파·접근성 확정) |
| 대상 독자 | 1인 개발자 본인 + Claude Code (구현 보조) |
| 엔진 | Unity 6 (6.3 LTS+), C# |
| 네트워크 | FishNet + Steamworks(Facepunch) |
| 문서 갱신 원칙 | 프로토타입(M1~M2) 검증 결과에 따라 수치는 전부 가변. "확정값"이 아니라 "시작값(baseline)"으로 취급한다 |

### 용어집 (요약, 상세는 부록 B)

| 용어 | 정의 |
|---|---|
| 파문 (SoundPulse) | 소리 발생 시 생성되는 시야 이벤트. 위치·반경·지속시간·타입을 가짐 |
| 리스너 (Seeker) | 술래 역할. 1인 |
| 도망자 (Runner) | 밸브를 열고 탈출해야 하는 역할. 3~5인 |
| 메아리 (Echo) | 태그당해 탈락한 도망자. 유령 상태로 계속 참여 |
| 밸브 (Valve) | 회전시켜 여는 탈출 게이트 트리거. 맵당 3개(6인은 3개, 회전시간 보정) |
| 차폐 (Occlusion) | 벽·지형지물에 의해 파문의 실제 인지 반경이 감쇠되거나 완전히 차단되는 현상 |
| 인지 배율 (Perception Multiplier) | 리스너의 역할에 따라 동일한 물리적 파문을 다르게 감지하는 배율 |

---

## 1. 게임 개요

| 항목 | 내용 |
|---|---|
| 가제 | 마르코! (MARCO!) |
| 장르 | 비대칭 음성 술래 호러 파티 |
| 인원 | 최소 2 / 권장 4~5 / 최대 6 (v1.x: 8~10인, 술래 2인) |
| 플랫폼 | PC(Steam), Windows |
| 한 판 길이 | 6~12분 (인원별 상이, 9절 참조) |
| 핵심 훅 | 말해야 앞이 보이는데, 말하면 술래가 듣는다 |
| 제작 전제 | 1인 개발(+Claude Code) · Unity 6 · Steam PC 유료 ₩6,000~15,000 · MVP 2~3개월/출시 6개월 내 · 호스트 기반 네트워킹 |

---

## 2. 핵심 재미 루프

### 2.1 30초 루프 (순간의 재미)

```
[불안: 암전] → [결단: 속삭임 or 한 걸음] → [정보: 파문으로 지형/술래 확인]
    → [대가: 방금 소리가 위치 광고] → (다시 침묵) → 반복
```

발화라는 인간의 기본 행동이 매 순간 리스크-리턴 도박이 되도록, 아래 3개 시스템이 이 루프를 지탱한다.

- **소리 등급 시스템** (5절) — 정보량과 노출량이 함께 커지는 구조
- **파문 렌더링** (5.3) — 0~80ms 이내 시각 피드백으로 "방금 그게 위험했다"를 즉시 체감
- **술래 파문 강화 인지** (3절, 5.7) — 같은 소리라도 술래에게는 실제로 더 멀리·더 오래 감지되는 기능적 우위로 작용

### 2.2 한 판 루프 (8~12분)

| 페이즈 | 길이(4인 기준) | 목표 | 종료 조건 |
|---|---|---|---|
| 은신 페이즈 | ~90초 | 지형·밸브 위치 파악, 초기 산개 | 첫 밸브 상호작용 시작 |
| 소음 작업 | ~5~7분 | 밸브 3개 회전 완료 | 밸브 3개 모두 열림 |
| 추격과 희생 | 병행 | 술래의 발각·태그, 메아리 전환 | 전원 태그 또는 시간 종료 |
| 탈출 러시 | 최종 1~2분 | 배수로 출구 도달 | 1인 이상 탈출 |

### 2.3 메타 루프 (재접속 동기)

- 술래 로테이션 3판 1세트 (~30분)
- 어워드 3종: 최다 비명상 / 무성 생존상 / 최고의 거짓말상 (v1.x: 통계 누적)
- 뮤테이터 투표로 같은 맵 문법 재조합 + 파문 스킨 해금

---

## 3. 역할(Role) 시스템 상세

### 3.1 역할별 파라미터

| 파라미터 | 리스너(술래) | 도망자 | 메아리(탈락자) |
|---|---|---|---|
| 기본 이동속도 | 5.0 m/s × 인원별 보정(1.05~1.12) | 5.0 m/s | 8.0 m/s (관전형, 콜라이더 없음) |
| 질주 이동속도 | 해당 없음(질주=상시 보정 반영) | 7.5 m/s | 해당 없음 |
| 파문 인지 배율 | 반경 ×1.2, 지속 ×1.5 (5.7절 — 실제 탐지 판정에 반영되는 기능적 수치) | 표준(×1.0) | 표준(관전 목적) |
| 방향 게이지 | 도망자 대화·고함 등급 발화 시 벽 너머 대략적 방향 표시(호스트 권위 계산, 상세는 3.4절) | 없음 | 없음 |
| 태그 판정 | 접촉 트리거, 반경 1.2m, 1회 접촉 즉시 확정 | 태그 대상 | 태그 불가(비활성 콜라이더) |
| 특수 능력 | 없음(순수 추적) | 잠수(풀맵 한정, 숨 게이지 8초, 회복 로직 5.6절 인접 표 참조) | 노크(30초 쿨다운, 3.2절) |
| 사망/탈락 처리 | 해당 없음 | 태그 1회 → 메아리로 즉시 전환 | 해당 없음(최종 상태) |

### 3.2 메아리(Echo) 상세 스펙

메아리는 "탈락 = 관전"이 아니라 "탈락 = 다른 방식의 참가"로 설계된 상태다.

- **이동**: 자유 비행형 유령 카메라, 충돌 없음(벽 통과), 이동속도 8.0 m/s
- **음성**: 생존자에게 들리지 않음. 메아리끼리는 별도 사망자 채널로 자유 대화
- **노크 능력**:
  - 쿨다운 30초, 사거리 제한 없음(맵 내 임의 지점 지정)
  - 조작: 미니맵/탑다운 뷰에서 지점 클릭 → 1.5초 지연 후 해당 지점에서 소음 발생
  - 발생 소음: "대화" 등급과 동일 취급 (반경 9m, 지속 1.2초) — 술래·도망자 모두 인지 가능
  - 전략적 의미: 도망자를 도와 술래를 유인하거나, 반대로 도망자를 낚아 술래에게 정보를 흘릴 수도 있음(플레이어 재량)
- **상태 전이**: `Runner --[Tagged]--> Echo` (역방향 전이 없음, 라운드 종료까지 유지)

### 3.3 상태 다이어그램 (역할)

```
[RoleAssign]
   ├─ Seeker(1) ──────────────────────────────► [InGame: Seeker]
   └─ Runner(N) ──────────────────────────────► [InGame: Runner]
                                                       │ Tagged (콜라이더 접촉)
                                                       ▼
                                                 [InGame: Echo]
                                                       │ RoundEnd
                                                       ▼
                                                 [ResultScreen]
```

### 3.4 방향 게이지 상세 스펙

3.1의 "방향 게이지"는 아래 규칙으로 확정한다. 이 규칙이 없으면 술래가 사실상 무제한 사거리로 도망자 위치를 아는 것처럼 오해될 소지가 있어 명확히 못박는다.

**트리거 조건**

- **속삭임 등급은 트리거하지 않는다.** 대화·고함 등급만 트리거된다 — "육성 발화"의 실제 정의는 이 둘뿐이다.
- 밸브 회전음, 발소리, 노크는 방향 게이지 트리거 대상에서 제외된다(이 게이지는 어디까지나 "말"에 대한 술래 전용 정보 우위).

**사거리 캡**

| 발화 등급 | 물리 반경(5.1) | 방향 게이지 최대 사거리(물리 반경 × 1.5) |
|---|---|---|
| 대화 | 9m | 13.5m |
| 고함 | 22m | 33m |

> 맵 전체(45m×35m, 대각선 약 57m)를 기준으로 고함조차 맵 전체를 커버하지 못하도록 설계해, 한 번의 실수가 맵 전체에 광고되는 최악의 시나리오를 방지한다.

**정밀도**

- 정확한 좌표가 아니라, 술래 기준 발화 지점 벡터를 8방위(N/NE/E/SE/S/SW/W/NW) 중 최근접 방위로 스냅해 화면 가장자리 링의 해당 구간만 밝힌다.
- 표시 지속시간은 5.7절에서 계산되는 `perceivedDuration`과 동일(고함 기준 최대 2.5초 × 1.5배 = 3.75초).
- 동일 발화자에 대한 게이지 갱신은 최소 0.5초 쿨다운을 둔다(판정 자체가 아니라 UI 재생 스팸 방지 목적).

---

## 4. 조작 및 카메라 시스템

### 4.1 시점

**1인칭 확정.** 사유:

- "완전한 흑 배경 + 파문"(16.1) 아트 컨셉은 3인칭보다 1인칭에서 몰입도가 높다 — 자기 캐릭터 실루엣이 화면을 가리지 않는다.
- 술래-도망자 간 접촉 판정(1.2m)이 짧아, 3인칭 카메라의 시야각 왜곡은 판정 체감을 해칠 수 있다.
- FOV: 기본 90°, 설정에서 75°~100° 조절 가능(12.6절).

### 4.2 캐릭터 컨트롤러

- Unity `CharacterController` 컴포넌트 기반. Rigidbody 물리 동기화는 사용하지 않는다(13.2 "물리 동기화 없음" 원칙과 일치).
- 이동 상태: `Idle / Walk(5.0m/s) / Sprint(7.5m/s, 도망자 전용) / Diving(수면 아래)` 4종.
- **크라우치는 MVP 미포함.** 이 게임은 시각 은신이 아니라 청각 은신이 핵심이라 크라우치의 이점이 모호하다. v1.x에서 "느린 이동으로 발소리 등급 자체를 낮추는" 형태로 재도입을 검토한다.
- 캐릭터 콜라이더 반경: 0.35m (태그 판정 1.2m와는 별개 — 순수 이동 충돌용).

### 4.3 키 바인딩 (기본값)

| 액션 | 기본 키(KBM) | 비고 |
|---|---|---|
| 이동 | WASD | — |
| 시점 회전 | 마우스 | Y축 반전 옵션(12.6절) |
| 질주 | Left Shift(홀드) | 이동 중에만 작동 |
| 상호작용(밸브·아이템 줍기) | E(홀드) | 6.1절 회전 3초와 매칭되는 홀드 게이지 UI 필요 |
| 아이템 사용(찰칵이) | Q 또는 마우스 우클릭 | 7절 무성 클릭 컨셉과 일치 |
| 발화(마이크) | 기본 상시 활성(VAD, 5.8절) | 별도 키 없음이 기본값 |
| 강제 마이크 뮤트 | Left Alt(홀드) | 실생활 인터럽트 대응(5.8절) |
| 잠수 | Left Ctrl(홀드, 수면 위에서만) | 뗄 시 자동 부상 |
| 메아리 전용: 미니맵/노크 지정 | Tab(미니맵 토글) + 좌클릭(지점 지정) | 3.2절 노크 UI 근거 |

- 컨트롤러 지원은 후순위(v1.x). 위 표는 KBM 전용 MVP 기준.

---

## 5. 사운드(SoundPulse) 시스템 — 핵심 메커니즘

이 게임에서 사운드는 곧 렌더러다. 시야를 만드는 유일한 수단이 소리이므로, 이 시스템이 사실상 게임의 전부다.

### 5.1 소리 등급 (Baseline)

| 소리 | 반경 | 지속시간 | 발생 조건 | 비고 |
|---|---|---|---|---|
| 속삭임 | 4m | 0.6초 | 마이크 RMS -50~-30 dBFS | 은신 정찰 기본기, 방향 게이지 트리거 안 됨 |
| 대화 | 9m | 1.2초 | 마이크 RMS -30~-15 dBFS | 협업용 표준 발화, 방향 게이지 트리거(3.4) |
| 고함 | 22m | 2.5초 | 마이크 RMS -15 dBFS 이상 | 본인 실루엣 하이라이트, 방향 게이지 트리거(3.4) |
| 걷기 | 2m | 0.4초 | 이동속도 ≤ 5.0m/s 상태에서 이동 | 재질별 배율(5.5) |
| 질주 | 6m | 0.8초 | 이동속도 > 5.0m/s | — |
| 밸브 회전 | 12m | 회전 내내(3초, 6인은 3.75초) | 밸브 상호작용 중 | 의도된 유인 장치 |
| 노크(메아리) | 9m | 1.2초 | 메아리 능력 사용 | "대화"와 동일 취급 |

### 5.2 음성 분류 파이프라인

```
1. 캡처   : Steamworks Voice API — 16kHz PCM, 20ms 프레임
2. 진폭   : 프레임별 RMS 산출 → dBFS 변환
3. 캘리브레이션 : 로비 입장 시 "가장 작은 목소리로 말해보세요" 3초 프롬프트
              → 개인별 baseline 측정 → 임계값에 개인 오프셋 적용
4. 대역 필터 : 80Hz~3,400Hz(인간 음성 주파수 대역)만 통과시키는 밴드패스 필터 적용
              → 팬 소음·키보드 타건음 등 저역/광대역 노이즈의 오분류 완화
5. 연속성 검사 : RMS가 임계값을 넘는 프레임이 최소 3프레임(60ms) 연속되어야 유효 발화로 인정
              → 순간적 임팩트 노이즈(마우스 클릭, 책상 두드림) 배제
6. 분류   : baseline 보정된 dBFS를 5.1 표의 3구간에 매핑
7. 디바운스 : 지속시간 0.15초 미만 발화는 무시(클릭·마찰음 오탐 방지)
8. 이벤트 생성 : SoundPulse(sourceId, position, radius, duration, type, timestamp)
```

> 캘리브레이션 없이 고정 임계값만 쓰면 마이크 감도 편차로 "속삭였는데 고함으로 분류"되는 사고가 빈발한다. 개인 baseline 보정은 MVP 필수 항목으로 승격한다. 대역 필터·연속성 검사는 캘리브레이션만으로 못 막는 환경 노이즈 오탐을 추가로 억제하기 위한 보강 단계다.

### 5.3 렌더링 이중화 (본인 vs 타인)

| 경로 | 지연 | 방식 |
|---|---|---|
| 본인 목소리 → 본인 화면 | 0ms | 로컬 마이크 진폭을 직접 셰이더 파라미터에 바인딩(네트워크 왕복 없음) |
| 본인 목소리 → 타인 화면 | 20~80ms | SoundPulse 이벤트 브로드캐스트 → 원격 클라이언트가 로컬 재현 |
| 환경음(걷기/밸브 등) | 20~80ms | 동일하게 이벤트 브로드캐스트 |

두 경로의 반경·지속시간·감쇠 곡선이 반드시 일치해야 한다. 불일치 시 "내가 보는 내 파문"과 "남이 보는 내 파문"이 달라져 눈치싸움의 공정성이 깨진다.

### 5.4 렌더링과 차폐(5.6)의 관계 — 이중 레이어 규칙

5.6에서 정의하는 차폐는 "탐지 가능 여부"(게임플레이 판정)이고, "화면에 무엇이 보이는가"(렌더링)는 아래처럼 별도의 이중 레이어로 확정한다.

| 레이어 | 판정 기준 | 표현 방식 |
|---|---|---|
| **판정 레이어**(필수) | 5.6 `ResolvePulseForListener`의 결과 | 서버가 "이 리스너는 이 이벤트를 알 수 있다"고 결론 내린 경우에만 이하 렌더링 진행 |
| **방향 힌트**(항상 표시) | 판정 레이어 통과 시 무조건 | 화면 가장자리 은은한 vignette로 발생 방향만 표시(정확한 위치 아님) — 벽 뒤에 있어도 "저쪽에서 뭔가 들렸다"는 정보만 제공 |
| **월드스페이스 링 이펙트**(조건부) | 방향 힌트 조건 + 카메라 Raycast로 실제 시야가 확보된 경우만 | 실제 3D 공간에 링이 그려짐 — 벽 뒤에서 난 소리는 "들렸다는 느낌"만 주고, 실제로 그 지점이 보이는 위치(모퉁이를 돈 뒤 등)에 도달해야 시각적으로 링이 나타난다 |

> 이 이중 레이어 덕분에 "벽 너머 소리가 났는데 마치 벽이 투명한 것처럼 링이 그려지는" 부자연스러움을 방지하면서도, 0~80ms 즉각 피드백 원칙(2.1)은 방향 힌트 레이어로 그대로 유지된다.

### 5.5 데이터 구조 (의사코드)

```csharp
// 이 구조체는 발생원(source) 기준 물리값만 담는다.
// 리스너별 인지 배율(5.7)과 차폐 감쇠(5.6)는 서버가 리스너마다 별도로 계산하며,
// 클라이언트에는 계산이 끝난 perceivedRadius/perceivedDuration만 전달된다.
struct SoundPulse {
    ulong   sourcePlayerId;
    Vector3 position;
    float   radius;       // m, 발생원 기준 원본값
    float   duration;     // s, 발생원 기준 원본값
    SoundType type;        // Whisper, Talk, Shout, Walk, Sprint, Valve, Knock
    float   timestamp;     // 서버 기준 시각
}

enum SoundType { Whisper, Talk, Shout, Walk, Sprint, Valve, Knock }

// 리스너에게 실제 전달되는 값 (서버 계산 결과, 5.6/5.7 참조)
struct PerceivedPulse {
    float   perceivedRadius;
    float   perceivedDuration;
    Vector3 sourcePos;
    bool    worldSpaceRingVisible; // 5.4 카메라 LOS 체크 결과
}
```

### 5.6 사운드 차폐(Occlusion) 규칙 — 핵심 확정 사항

5.1의 반경 수치는 "빈 공간 기준 최대값"이다. 실제 전파는 지형에 의해 감쇠된다. 이 규칙이 없으면 10·11절의 "은신처"·"파문 차단막" 설계가 기계적으로 무의미해지므로 최우선으로 명시한다.

**판정 알고리즘 (서버 권위, 리스너별 개별 계산)**

```
function ResolvePulseForListener(pulse, listener):
    straightDist = Distance(pulse.position, listener.position)

    # 1) 역할별 인지 배율 적용 (5.7) — 술래는 여기서 먼저 확장된 반경을 갖는다
    baseRadius = pulse.radius * RoleRadiusMultiplier(listener.role)
    baseDuration = pulse.duration * RoleDurationMultiplier(listener.role)

    # 2) 1차 컷: 배율 적용 후에도 물리적으로 닿지 않으면 즉시 스킵 (대역폭 절약)
    if straightDist > baseRadius:
        return null  # 이 리스너에게는 이벤트 자체를 전송하지 않음

    # 3) 차폐 레이캐스트: pulse.position → listener.position 사이 SoundBlocking 레이어 검사
    hits = Physics.LinecastAll(pulse.position, listener.position, layer="SoundBlocking")

    if hits contains tag "HardBlocker":       # 무대 커튼 등 (11.1)
        return null                            # 완전 차단, 이벤트 미전송
    else:
        wallCount = hits.count                 # 일반 벽/문 개수
        attenuation = 0.5 ^ wallCount          # 벽 1개당 반경 50%씩 감쇠
        perceivedRadius = baseRadius * attenuation
        perceivedDuration = baseDuration * max(attenuation, 0.5)  # 지속시간은 최대 50%까지만 깎임

        if straightDist > perceivedRadius:
            return null
        return PerceivedPulse(perceivedRadius, perceivedDuration, pulse.position)
```

**지형지물별 차폐 태그 매핑**

| 지형지물 | 태그 | 효과 |
|---|---|---|
| 일반 벽(기본 지오메트리) | `Wall` | 통과 시 반경 ×0.5 (중첩 가능, 벽 2개면 ×0.25) |
| 락커(10.1 은신처) | `Wall`(얇은 오브젝트 취급) | 동일 규칙, 콜라이더는 락커 사이 틈만 노출되도록 배치해 시각적 은신 연출과 일치시킴 |
| 무대 커튼(11.1) | `HardBlocker` | 완전 차단(이벤트 자체 미전송) — 유일한 절대 차단 지형지물 |
| 문(닫힘 상태, v1.x) | `Wall` | 열림 상태는 차폐 없음(콜라이더 비활성) |
| 수면 아래(잠수) | 해당 없음 | 애초에 파문 미발생(5.5절 재질 표), 이 규칙과 무관 |

> **성능 고려(15.5 연계)**: 리스너별 레이캐스트 비용은 `초당 파문 발생 수 × 접속 인원 수`에 비례한다. 6인 기준 초당 파문 2~4개 × 리스너 5명 ≈ 초당 10~20회 레이캐스트로, `Physics.Linecast`가 충분히 감당 가능한 수준이다. `SoundBlocking` 레이어는 렌더 메시와 분리된 전용 콜라이더 레이어로 둬서 다른 물리 연산(플레이어 이동 충돌 등)과 섞이지 않게 한다.

### 5.7 리스너별 인지 배율 적용 방식

**인지 배율은 연출이 아니라 실제 탐지 판정에 반영되는 기능적 수치다.** 같은 소리라도 술래는 실제로 더 멀리, 더 오래 감지한다(2.1의 핵심 3대 시스템 중 하나로 명시된 것과 일치).

```
[Source] --물리적 이벤트(5.1의 원본 반경/지속)--> [Server]
    │
    ├─ Listener A(Runner) : radius × 1.0, duration × 1.0  → 5.6 차폐 계산 → 전송 여부 결정
    ├─ Listener B(Seeker) : radius × 1.2, duration × 1.5  → 5.6 차폐 계산 → 전송 여부 결정
    └─ Listener C(Echo)   : radius × 1.0, duration × 1.0  → (관전 목적, 정보 참고용 동일 적용)
```

`SoundPulse` 구조체(5.5)는 발생원 기준 반경/지속만 담는다. 리스너별 배율·차폐 적용은 서버가 매번 계산하며, 클라이언트로는 계산이 끝난 `PerceivedPulse`만 내려간다. 클라이언트는 받은 값을 그대로 렌더링 크기·지속시간에 사용하고 추가 배율 연산을 하지 않는다 — 이렇게 해야 5.3의 "내가 보는 내 파문 vs 남이 보는 내 파문 일치 원칙"이 역할별 배율과 충돌하지 않는다.

### 5.8 음성 입력 방식 및 오탐 방지

**기본 방식: Voice Activity Detection(VAD) 오픈마이크 + 수동 강제 뮤트.**

- PTT(누르고 말하기)를 기본값으로 채택하지 않는다. 이유: 핵심 훅("말해야 보인다")은 무의식적으로 말이 새어나가는 순간의 공포가 핵심이므로, 버튼을 눌러야만 소리가 나는 PTT는 이 훅을 구조적으로 무력화한다.
- 실생활 인터럽트(전화, 다른 사람과의 대화 등) 대응을 위해 **Left Alt 홀드 = 강제 마이크 뮤트** 키를 항상 제공한다. 이 키를 누르는 동안은 어떤 소리도 SoundPulse로 변환되지 않는다.
- 오탐 방지는 5.2 파이프라인의 대역 필터(4단계)·연속성 검사(5단계)로 처리한다.
- 설정 화면(12.6)의 "게임플레이" 탭에서 "발화 방식: VAD(기본) / PTT(대체)" 토글을 제공한다. PTT 선택 시 "게임 핵심 긴장감이 줄어든다"는 안내 문구를 노출한다.

### 5.9 재질별 발소리 배율

| 지형 | 배율 |
|---|---|
| 타일(샤워장, 지하통로) | ×1.3 |
| 나무 마루(폐극장 지하) | ×1.2, 상시 강제 발생(은신 불가) |
| 매트/카펫 | ×0.7 |
| 물(풀 수면 아래) | 은신 보정 — 파문 발생 안 함, 대신 숨 게이지 8초 소모(회복 로직은 아래 표) |

**잠수 게이지 회복 로직**

| 상태 | 규칙 |
|---|---|
| 수면 아래(잠수 중) | 게이지 초당 -1 (8초에 0 도달) |
| 자발적 부상(게이지 남은 상태로 스스로 올라옴) | 부상 완료 2초 후부터 초당 +2 회복(완전 회복까지 최대 4초), 물 밖으로 완전히 나가면 즉시 100% 회복 |
| 강제 부상(게이지 0 도달) | **"질식" 상태 3초**: 이동속도 -20%, 강제로 "고함"과 동일한 반경/지속의 SoundPulse 1회 발생(기침·헐떡임 연출) — 잠수의 대가를 명확히 함 |

---

## 6. 밸브 · 탈출 시스템

### 6.1 밸브 상태기계

```
[Closed] --interact(hold)--> [Rotating(3s, 소음 12m 발생)] --완료--> [Open]
   ▲                               │
   └───────interrupt(이탈)─────────┘   (진행도 초기화, 완료 전 이탈 시 리셋)
```

- 밸브 회전 중 소음(12m/3초)이 지속 발생 — 중간에 멈춰도 이미 발생한 소음은 취소되지 않음
- 완료 전 상호작용 취소 시 진행도는 0으로 리셋(부분 진행 저장 없음, MVP 단순화)
- 밸브 3개 모두 `Open` → 배수로 게이트 `Open` → 탈출 가능

### 6.2 인원별 밸런스 (baseline)

| 인원 | 도망자 | 밸브 수 | 술래 이속 보정 | 제한시간 | 구현 상태 |
|---|---|---|---|---|---|
| 2 | 1 (AI 술래 권장) | 2 | +5% | 6분 | v1.x — AI 술래 구현 후에만 유효 |
| 3 | 2 | 2 | +6% | 8분 | v1.x |
| **4** | **3** | **3** | **+8%** | **10분** | **✅ MVP 대상(유일하게 지금 구현)** |
| 5 | 4 | 3 | +10% | 10분 | v1.x |
| 6 | 5 | 3(회전시간 +25% → 3.75초) | +12% | 12분 | v1.x |

> M3(MVP, 21절 5~10주차)에서는 **4인 행만 구현한다.** 나머지 행은 9절 밸런스 산출 로직을 미리 남겨두기 위한 참고 자료이며 이번 스프린트의 구현 대상이 아니다.

### 6.3 승패 판정 (의사코드)

```csharp
if (valvesOpened == totalValves && runnersEscaped >= 1)
    result = Result.RunnersWin;
else if (allRunnersTagged || timeRemaining <= 0)
    result = Result.SeekerWin;
```

목표: 술래 승률 40~60% 구간. MVP 플레이테스트에서 이 구간을 벗어나면 9절 수치부터 조정한다.

### 6.4 밸브 상호작용 중 연결 끊김 처리

| 상황 | 처리 |
|---|---|
| 밸브 회전 중 상호작용자 연결 끊김 | 진행도 즉시 0으로 리셋(6.1 "완료 전 이탈" 규칙과 동일하게 취급) |
| 밸브 자체 잠금 여부 | 잠그지 않음 — 다른 생존 도망자가 즉시 이어서 상호작용 시작 가능(친구 간 플레이 특성상 어뷰징 우려보다 흐름 끊김 방지가 우선) |
| 해당 슬롯 처리 | 13.3 규칙대로 메아리로 즉시 전환 |

---

## 7. 아이템 시스템

| 아이템 | 스펙 | 상호작용 | 리스폰 |
|---|---|---|---|
| 찰칵이 | 3회 충전, 무성 클릭으로 반경 6m 즉시 밝힘(파문 이벤트 없음 — 사운드 시스템과 별개 처리) | 즉시 사용(쿨다운 없음, 충전 소모) | 맵당 4곳, 90초 |
| 소리통 (v1.x) | 목소리 3초 녹음 → 설치 → 원격 재생. 세션 메모리에만 존재, 판 종료 시 삭제 | 설치 후 원격 트리거(재생 시 "대화" 등급 파문 발생) | 맵당 2곳 |
| 고무오리 (v1.x) | 투척형, 착지 지점에서 유인음 발생 | 투척 → 착지 시 "대화" 등급 파문 1회 | 맵당 3곳 |

> MVP에는 찰칵이 1종만 포함(20절 참조). 소리통·고무오리는 v1.0 정식 출시 시점 콘텐츠.

---

## 8. 승패 · 라운드 종료 처리

- 결과 화면 전환 즉시 어워드 3종 판정(최다 비명상/무성 생존상/최고의 거짓말상 — 판정 기준은 세션 로그의 SoundType.Shout 발생 횟수, 이동거리 대비 파문 발생 0회 여부, 메아리 노크 성공 유인 횟수로 각각 산출)
- 리매치 투표: 15초 카운트다운, 과반 찬성 시 즉시 재시작(역할 재추첨)

---

## 9. 밸런스 설계 근거 (수치 산출 로직)

이 절은 5·6절의 숫자가 "왜 이 값인가"를 설명한다. 모두 baseline이며 M3(MVP) 플레이테스트에서 검증·조정 대상이다.

### 9.1 소리 반경 스케일링 — 왜 4m / 9m / 22m인가

- 반경 비율: 속삭임:대화:고함 = 1 : 2.25 : 5.5
- "대화"까지는 협업에 필요한 최소 비용으로 느끼게 하고, "고함"부터는 명백한 실책으로 체감되도록 격차를 크게 벌린다.
- 노출량(반경²×지속시간, 즉 파문이 덮는 시공간)을 기준으로 계산하면 고함은 속삭임 대비 약 **120배**(면적 30배 × 지속시간 4배)에 달한다. 실수 하나의 대가가 명확히 느껴지도록 의도한 비선형 설계다.

### 9.2 술래 이속 보정 — 왜 4인 기준 +8%인가

- 도망자 기본속도 5.0m/s, 술래 5.4m/s(+8%) → 상대속도 0.4m/s
- 술래가 도망자를 20~25m 거리에서 발견했다고 가정하면, 상대속도만으로 좁혀지는 데 약 **50~62초** 소요
- 한 판 8~12분 중 추격 국면이 과도하게 길어지지 않으면서도, 도망자가 지형지물과 파문 재생성 텀(0.6~2.5초)을 활용해 따돌릴 여지를 남기는 균형점

### 9.3 밸브 회전 3초 / 소음 12m — 왜 이 조합인가

- 12m 반경 소음이 3초간 지속 → 술래가 그 반경 안에 있다면 도착 시간(12m ÷ 5.4m/s ≈ 2.2초)이 회전 완료 시점(3초)과 겹쳐, 인터셉트가 유의미한 확률로 발생
- 즉 밸브 작업은 "안전한 루틴"이 아니라 매번 "술래를 부를지 말지"를 재는 도박으로 설계됨(15.5절 성능·9절 밸런스 원칙과 직결)

### 9.4 인원별 밸브 수·이속 보정 조정 로직

- 밸브 수를 3개로 고정한 이유: 도망자가 늘어도(3~5인) 병렬 작업 상한을 3으로 묶어, 인원이 많을수록 클리어가 과도하게 쉬워지는 것을 방지
- 대신 인원이 늘수록 술래 이속을 +5%→+12%로 단계적으로 높여 난이도를 유지
- 6인 구간에서 회전시간을 3초→3.75초(+25%)로 늘린 것은, 늘어난 인력이 밸브 작업 병목을 지나치게 빨리 해소하는 것을 상쇄하는 보정

### 9.5 목표 승률 40~60%의 의미

- 어느 한쪽이 상시 우세하면 "술래 하기 싫어서" 로테이션 동기가 무너지고, 파티게임 특유의 반복 재플레이가 죽는다
- 튜닝 우선순위: ① 술래 이속 보정 → ② 밸브 회전시간 → ③ 밸브 반경/소리 등급 → ④ 맵 지형(마지막 수단, 재작업 비용 큼)

---

## 10. 맵 설계 — 심야 실내수영장 (MVP 대상)

전체 플레이 구역 약 45m × 35m. 그레이박스 1주 + 미감 2주(총 3주) 규모.

### 10.1 구역 구성표

| 구역 | 규모(대략) | 핵심 기능 | 사운드 특성 | 연결 구역 |
|---|---|---|---|---|
| 입구 로비 | 8×6m | 도망자 스폰 지점(술래는 격리 공간에서 3초 후 별도 진입) | 표준 | 라커룸 A/B, 메인 풀 홀 |
| 라커룸 A/B | 각 6×5m | 락커 사이 은신처(하드커버, 5.6 `Wall` 태그 — 반경 ×0.5 감쇠) | 표준 | 로비, 샤워장 |
| 샤워장 | 5×5m | 통로 겸 소형 은신처 | 타일 반향 — 발소리 파문 ×1.3 | 라커룸, 메인 풀 홀 |
| 메인 풀 홀 | 20×14m | 중앙 허브, 수면 백색소음존 | 물소리 상시 앰비언스로 발소리 부분 은폐. 잠수 시 파문 미발생(숨 게이지 8초) | 로비, 샤워장, 기계실, 라이프가드실, 보일러 통로 |
| 기계실 | 7×6m | **밸브 #1**(필터 밸브) | 상시 기계 앰비언스(중간 백색소음) | 메인 풀 홀(단일 출입구 — 막다른 방, 고위험) |
| 라이프가드실 | 5×4m | **밸브 #2**(비상 배수 밸브) | 표준. 유리창으로 풀 홀 파문 관측 가능(시각 정보 흐름) | 메인 풀 홀(단일 출입구) |
| 보일러 통로 | 3×10m(협소) | **밸브 #3**(보일러 밸브) | 좁은 통로 — 발소리 파문 반사 ×1.2 | 메인 풀 홀, 배수로 출구 |
| 배수로 출구 | 4×6m | 밸브 3개 완료 시 게이트 개방, 탈출 지점 | 표준 | 메인 풀 홀, 보일러 통로 |

### 10.2 설계 의도

- 밸브 3개가 각각 **막다른 방(기계실·라이프가드실)** 과 **협소 통로(보일러)** 에 배치되어, 밸브 작업 자체가 퇴로 없는 리스크를 안게 설계됨(도망 경로가 제한적)
- 메인 풀 홀은 모든 구역을 잇는 허브이자 유일한 "안전 은폐 자원"(수면)을 가진 공간 — 자연스럽게 교전이 집중되는 지점
- 라이프가드실의 유리창은 청각 정보(파문)를 시각 정보로 보조하는 유일한 지형지물 — 향후 뮤테이터/난이도 조절 지렛대로 활용 가능
- 락커·벽 등 구역 경계는 전부 5.6절 `Wall` 태그가 적용되어, 인접 구역으로 소리가 그대로 새지 않고 통과당 반경이 절반씩 줄어든다

### 10.3 플레이 동선 예시 (내부 검증용 시나리오)

1. 도망자 3인, 로비 스폰 직후 각자 라커룸/풀 홀/보일러 통로로 분산 정찰(은신 페이즈)
2. 한 명이 기계실로 진입해 밸브 회전 시작 → 12m 반경 소음 발생
3. 술래가 메인 풀 홀에서 소음을 인지, 기계실로 이동(2.2초 내 도착 가능 거리였다면 인터셉트 성공 확률 높음)
4. 다른 도망자가 반대편 보일러 통로에서 밸브 작업 개시 — 술래의 주의 분산 유도
5. 태그 발생 시 해당 도망자 메아리로 전환, 30초 후 노크로 남은 도망자에게 술래 위치 힌트 제공(혹은 낚시)

---

## 11. 맵 설계 — 폐극장 (v1.0 콘텐츠)

전체 플레이 구역 약 40m × 30m. 그레이박스+미감 3주 규모.

### 11.1 구역 구성표

| 구역 | 규모(대략) | 핵심 기능 | 사운드 특성 | 연결 구역 |
|---|---|---|---|---|
| 로비 | 8×6m | 스폰 지점 | 표준 | 객석 |
| 객석 | 18×16m | 대형 개방구역, 좌석 열이 은신+시야 차단 하드커버 | 표준 | 로비, 무대, 조명실 계단 |
| 무대 | 14×10m | 대형 커튼 = **파문 차단막**(5.6 `HardBlocker` 태그, 유일한 완전 차단 지형지물) | 표준 | 객석, 분장실 A/B, 소품보관실 |
| 분장실 A/B | 각 5×5m | 거울·소품 은신처 | 표준 | 무대 |
| 소품보관실 | 6×6m | **밸브 #1** | 표준(소품 충돌 SFX 존재) | 무대 |
| 조명실/그리드 | 수직 구조, 좁은 통로+사다리 | **밸브 #2**, 진입로 1개뿐(고립 리스크) | 표준 | 객석 계단(단일 연결) |
| 무대 아래 지하통로 | 협소, 나무 바닥 | **밸브 #3** | **삐걱대는 마루 — 이동 시 강제 "걷기" 소리 발생, 은신 불가**(하이리스크·하이리워드 지역) | 무대, 비상구 |
| 비상구 | 4×5m | 탈출 지점 | 표준 | 지하통로 |

### 11.2 설계 의도

- 커튼(파문 차단막)은 심야 수영장에 없던 신규 지형지물 — 소리를 물리적으로 완전 차단할 수 있는 유일한 사례(5.6절 `HardBlocker`)로, "숨는다"의 정의를 확장
- 조명실/그리드는 진입로가 하나뿐이라 밸브 작업 중 퇴로가 없음 — 6.1 밸브 시스템의 리스크를 극단화한 배치
- 지하통로는 은신이 원천 봉쇄된 구간으로 설계해, 같은 밸브 메커니즘이라도 맵마다 완전히 다른 텐션을 만들도록 함(맵 1~2개로 반복 소비 가능하게 하는 장치)

---

## 12. UI/UX 와이어프레임

공통 원칙: 화면의 주인공은 항상 어둠과 파문이다. HUD는 최소주의를 유지하고, 모든 정보는 텍스트보다 파문/색으로 전달한다.

### 12.1 타이틀 화면

```
┌──────────────────────────────────────────────┐
│                                                │
│                                                │
│                마르코!  MARCO                  │
│         (배경: 파문 애니메이션 상시 루프)         │
│                                                │
│                                                │
│               ┌──────────────┐                │
│               │   시작하기    │                │
│               └──────────────┘                │
│                                                │
│  v0.2.0                          설정 ⚙        │
└──────────────────────────────────────────────┘

최초 실행 시 오버레이 (모달):
┌────────────────────────────────┐
│      마이크 권한이 필요합니다      │
│  이 게임은 목소리로 앞을 봅니다     │
│                                  │
│   [ 마이크 허용 ]     [ 나중에 ]  │
└────────────────────────────────┘
```

| 요소 | 위치/크기 | 상태 | 동작 |
|---|---|---|---|
| 로고 | 화면 상단 중앙, 대형 | 상시 | 파문 배경 루프 애니메이션(장식용, 클릭 불가) |
| 시작하기 버튼 | 화면 중앙 | Default / Hover(파문 확산 효과) / Pressed | 클릭 시 마이크 미허용이면 권한 모달 우선 표시, 허용 시 메인 메뉴로 |
| 마이크 권한 모달 | 화면 중앙, 오버레이 | 최초 실행 1회만 | "마이크 허용" 클릭 시 OS 권한 요청 → 메인 메뉴 진입. "나중에" 클릭 시 찰칵이 전용 안내 문구와 함께 진입 허용 |
| 버전 정보 | 좌하단, 소형 텍스트 | 상시 | 표시만, 인터랙션 없음 |

### 12.2 메인 메뉴

```
┌──────────────────────────────────────────────┐
│  마르코!                                       │
│                                                │
│   ┌────────────┐  ┌────────────┐              │
│   │  방 만들기  │  │  코드 입장  │              │
│   └────────────┘  └────────────┘              │
│                                                │
│   ┌────────────────────────┐                  │
│   │      솔로 연습장         │                  │
│   └────────────────────────┘                  │
│                                                │
│   설정          종료                            │
└──────────────────────────────────────────────┘
```

| 요소 | 동작 |
|---|---|
| 방 만들기 | 호스트로 로비 생성 → 4자리 방코드 발급 → 로비 화면 전환 |
| 코드 입장 | 4자리 코드 입력 필드 노출 → 유효성 검사 → 로비 화면 전환 |
| 솔로 연습장 | AI 없이 혼자 파문/소리 시스템만 체험하는 오프라인 씬(마이크 캘리브레이션 겸용) |

### 12.3 로비 — "발화로 밝아지는 방"

```
┌──────────────────────────────────────────────┐
│  방코드: 7 3 K 9                뮤테이터 투표 ▾ │
│                                                │
│      ○ 동현       ○ 플레이어2                  │
│    (말하면 파문 확산, 마이크 레벨 실시간 반영)     │
│                                                │
│      ○ 플레이어3       ○ 플레이어4              │
│                                                │
│  ┌──────────────────────────────────┐         │
│  │  준비완료 (2/4)      [ 준비 ]      │         │
│  └──────────────────────────────────┘         │
└──────────────────────────────────────────────┘
```

| 요소 | 위치/크기 | 상태 | 동작 |
|---|---|---|---|
| 방코드 | 상단 좌측 | 상시 노출 | 클릭 시 클립보드 복사 |
| 뮤테이터 투표 | 상단 우측 드롭다운 | 접힘/펼침 | 후보 중 다수결(동률 시 호스트 결정) — v1.x |
| 플레이어 원형 아바타 | 중앙 그리드 배치, 인원수만큼 | 발화 시 마이크 진폭에 비례해 원 테두리가 파문처럼 확산 | 마이크 캘리브레이션 겸 조작 학습 — 이 화면 자체가 튜토리얼 |
| 준비 버튼 | 하단 | Default / Ready(체크) | 전원 Ready 시 3초 카운트다운 후 역할 추첨 연출로 전환 |

### 12.4 인게임 HUD

```
┌──────────────────────────────────────────────┐
│ ⚙ 0/3                                          │
│                                                │
│                                                │
│              (화면 전체 = 암전 + 파문)           │
│                                                │
│                                                │
│                                    ▮ 찰칵이 x3  │
│              ▂▂▂▃▅ (잠수 중에만 노출)            │
└──────────────────────────────────────────────┘
```

| 요소 | 위치/크기 | 노출 조건 | 동작 |
|---|---|---|---|
| 밸브 카운트 | 좌상단, 소형 | 상시(인게임 중) | 밸브 열릴 때마다 숫자 증가 + 짧은 파문 이펙트 |
| 아이템 슬롯 | 우하단, 1슬롯 | 상시 | 찰칵이 보유 시 아이콘+충전수 노출, 클릭/키 입력으로 사용 |
| 숨 게이지 | 하단 중앙 | 수면 잠수 중에만 | 8초에서 0으로 감소하는 바, 0 도달 시 강제 부상(5.9절 페널티 적용) |
| 방향 게이지(술래 전용) | 화면 가장자리 링 | 술래 역할 + 도망자 대화·고함 등급 발화 발생 시에만(3.4) | 소리 발생 방향으로 가장자리가 8방위 스냅으로 밝아짐(호스트 권위 계산) |
| 파문 자막(접근성, 옵션) | 화면 하단 구석 | 12.6 접근성 옵션 켜짐 시 | "속삭임/대화/고함" 등 파문 유형 텍스트 짧게 노출 |

첫 20초는 강제 오프닝 가이드(내 발소리 확인 → 첫 속삭임 유도)로 "아무것도 안 보여서 껐다"를 차단한다. 마우스+키보드 우선, 컨트롤러는 후순위.

### 12.5 결과 화면

```
┌──────────────────────────────────────────────┐
│              도망자 승리                        │
│                                                │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐    │
│  │ 최다 비명상 │ │무성 생존상 │ │최고의거짓말상│    │
│  │  플레이어2  │ │  동현      │ │  플레이어3  │    │
│  └───────────┘ └───────────┘ └───────────┘    │
│                                                │
│   ┌──────────────────────┐                    │
│   │ ▶ 사운드맵 타임랩스 재생 │   [ 클립 저장 ]    │
│   └──────────────────────┘                    │
│                                                │
│         리매치? (3/4 찬성)   15초  [찬성]        │
└──────────────────────────────────────────────┘
```

| 요소 | 동작 |
|---|---|
| 승패 배너 | 승리 진영 색(도망자=시안 / 술래=레드, 색맹 모드는 16.2 대체 팔레트)으로 대형 텍스트 |
| 어워드 카드 3종 | 8절 판정 로직 결과를 카드형으로 노출 |
| 사운드맵 타임랩스 | 해당 판의 모든 SoundPulse 로그를 30초로 압축 재생(포스트 MVP) |
| 클립 저장 버튼 | 타임랩스를 mp4/gif로 로컬 저장(포스트 MVP) |
| 리매치 투표 | 15초 카운트다운, 과반 찬성 시 즉시 재시작 |

### 12.6 설정 화면 상세 스펙

```
┌──────────────────────────────────────────────┐
│  [오디오]  [비디오]  [조작]  [게임플레이]         │
│                                                │
│  (선택된 탭 내용)                                │
│                                                │
│                              [ 기본값 복원 ] [닫기]│
└──────────────────────────────────────────────┘
```

**오디오 탭**

| 항목 | 유형 | 기본값 |
|---|---|---|
| 마이크 장치 선택 | 드롭다운(OS 인식 장치 목록) | 시스템 기본 장치 |
| 입력 게인 | 슬라이더(-20dB~+20dB) | 0dB |
| 발화 감도 재보정 | 버튼 → 3초 프롬프트(5.2절) 재실행 | — |
| 마스터 볼륨 | 슬라이더 | 80% |
| 효과음/UI 볼륨 | 슬라이더(개별) | 80% |

**비디오 탭**

| 항목 | 유형 | 기본값 |
|---|---|---|
| FOV | 슬라이더(75°~100°) | 90° |
| 밝기(파문 대비) | 슬라이더 | 중간 |
| 색맹 모드 | 토글(끔/적록색맹 대응, 16.2절) | 끔 |
| 프레임 표시 | 토글 | 끔 |

**조작 탭**

| 항목 | 유형 |
|---|---|
| 키 리바인딩 | 4.3절 표 전체 재배정 가능 |
| 마우스 감도 | 슬라이더 |
| Y축 반전 | 토글 |

**게임플레이 탭**

| 항목 | 유형 | 기본값 |
|---|---|---|
| 발화 방식(VAD/PTT) | 토글(5.8절) | VAD |
| 파문 유형 자막(접근성) | 토글 | 끔 |
| 강제 오프닝 가이드 재시청 | 버튼 | — |

---

## 13. 멀티플레이 세션 설계

### 13.1 세션 흐름

```
방 생성(호스트) → 방코드/오버레이 초대 → 로비 → 역할 추첨 연출(3초)
   → 인게임 → 결과·리매치 투표 → (로비 복귀 또는 즉시 재시작)
```

### 13.2 보이스챗 정책

- 인게임 근접 VoIP가 유일한 시야 생성원. 디스코드 등 외부 보이스로 말해도 화면이 밝아지지 않으므로 구조적으로 무력화됨("디스코드 금지" 안내 불필요)
- 메아리(탈락자)는 별도 사망자 채널에서만 대화 가능

### 13.3 난입 · 이탈 처리

| 상황 | 처리 |
|---|---|
| 진행 중 난입 | 관전 합류만 허용(다음 라운드부터 참가 가능) |
| 도망자 이탈 | 해당 슬롯 즉시 메아리 전환(6.2 밸런스 표는 유지, 재계산 없음) |
| 밸브 회전 중 이탈 | 6.4절 참조(진행도 리셋, 밸브 즉시 재개방 가능) |
| 술래 이탈 | 판 무효 처리 후 역할 재추첨 |
| 호스트 이탈 | 세션 종료. 호스트 마이그레이션은 v1.x 스코프(MVP 미포함) |

---

## 14. 네트워크 아키텍처 & 이벤트 프로토콜

### 14.1 스택 선정

| 옵션 | 채택 여부 | 사유 |
|---|---|---|
| **FishNet + Steamworks(Facepunch)** | ✅ 채택 | 무료·CCU 무제한·활발한 유지보수·클라이언트 예측 내장. 필요 시 Steam Datagram Relay 경유 |
| Mirror | 차선 | 레퍼런스는 많으나 FishNet 대비 우선순위 낮음 |
| Photon Fusion | ❌ 배제 | 틱 정밀 동기화용 과스펙 + 동접 과금 구조가 1인 유료 게임과 상충 |

### 14.2 동기화 설계 원칙

- **물리 동기화 없음.** 동기화 대상은 ① 플레이어 위치·회전(10~20Hz 보간), ② SoundPulse/PerceivedPulse 이벤트뿐
- 파문 렌더는 각 클라이언트가 로컬 재현하므로 20~80ms 지연은 체감되지 않음
- 밸런스는 위치+이벤트 트래픽만으로 성립하도록 설계되어(5절), 하드 넷코드 요구사항을 구조적으로 회피
- 5.6절 차폐 계산은 서버에서 수행되며, 차폐로 인해 애초에 도달하지 않는 이벤트는 해당 클라이언트로 아예 전송되지 않아 트래픽이 자연히 절감됨

### 14.3 이벤트 프로토콜 상세

| 이벤트 | 발신 | 수신 | 페이로드 | 트리거/빈도 |
|---|---|---|---|---|
| JoinRoom | Client → Server | Server | `{roomCode, playerName}` | 로비 입장 1회 |
| ReadyToggle | Client → Server | Server → All | `{playerId, isReady}` | 유저 액션 시 |
| MutatorVote (v1.x) | Client → Server | Server → All | `{playerId, mutatorId}` | 유저 액션 시 |
| RoleAssigned | Server → All | All | `{playerId, role}` | 라운드 시작 1회 |
| SoundPulse | Client → Server | Server(내부 판정용) | `{sourceId, position, radius, duration, type, timestamp}` | 발화/이동/상호작용마다(4인 평균 초당 2~4개, ~40byte) |
| PerceivedPulse | Server → 해당 리스너만 | 개별 클라이언트 | `{perceivedRadius, perceivedDuration, sourcePos, worldSpaceRingVisible}` | 5.6 판정 통과 시에만 개별 전송 |
| ValveRotateStart/Progress/Opened | Client → Server → All | All | `{valveId, playerId, progress}` | 상호작용 시작/3초간/완료 |
| PlayerTagged | Server → All | All | `{seekerId, targetId, timestamp}` | 태그 판정 시 |
| EchoKnock | Client → Server → All | All | `{echoPlayerId, targetPosition}` | 30초 쿨다운 사용 시 |
| ItemUse | Client → Server → All | All | `{playerId, itemId, position}` | 아이템 사용 시 |
| GameEnd | Server → All | All | `{result, awards[]}` | 라운드 종료 시 |
| HostMigration | — | — | — | v1.x 스코프, MVP 제외 |

> SoundPulse가 트래픽의 대부분을 차지하나, 페이로드가 작고(~40byte) 빈도가 낮아(초당 개자릿수) 대역폭 부담은 미미하다. 위치 보간은 별도 스트림(10~20Hz)으로 처리. `PerceivedPulse`는 리스너별 개별 전송이라 원본 `SoundPulse`보다 오히려 트래픽이 분산·절감된다(차폐로 걸러진 리스너는 아예 수신하지 않음).

### 14.4 까다로운 지점 3가지

1. 본인 목소리 파문의 0지연 로컬 렌더 vs 타인에게 도달하는 이벤트 경로의 강도·타이밍 일관성 유지 (5.3절)
2. Steam Voice 원시 PCM 접근 → 진폭 추출 → 거리 감쇠·3D 패닝, 그리고 5.6절 리스너별 차폐 레이캐스트를 매 이벤트마다 감당하는 서버 부하
3. 술래 방향 게이지의 호스트 권위 계산(클라 조작 여지 축소) — 단, 친구 대상 게임이므로 안티치트는 과투자하지 않음

---

## 15. 기술 아키텍처 (매니저별 스펙)

### 15.1 씬 구성

```
Boot → MainMenu → Lobby → Game(맵별 어디티브 로드)
```

### 15.2 폴더 구조

```
_Project/{Scripts, Shaders, Prefabs, Maps, Audio, UI, Settings}

어셈블리 디피니션 3개:
  Core          — 게임 로직, 상태기계, 밸런스 수치, 차폐 판정
  Net           — NetworkBridge, 이벤트 직렬화
  Presentation  — UI, 파문 렌더링, 사운드 재생

(Claude Code의 작업 단위·컨텍스트 경계와 일치시키기 위한 분리)
```

### 15.3 매니저별 상세 스펙

| 매니저 | 책임 | 핵심 상태/데이터 | 비고 |
|---|---|---|---|
| **GameFlowManager** | 라운드 상태기계 관리 | `Boot → MainMenu → Lobby → RoleAssign → InGame → RoundEnd → (Lobby ⇄ Rematch)` | 15.4 상태표 참조 |
| **NetworkBridge** | FishNet 래퍼, 넷 라이브러리 교체 대비 격리층 | 14.3 이벤트 테이블 전체를 이 계층에서 송수신 | Core가 직접 FishNet API를 참조하지 않도록 격리 |
| **VoiceSystem** | 마이크 캡처·진폭 추출·공간화 | 5.2 파이프라인, 개인별 baseline 오프셋, 5.8 VAD/PTT 전환 | Steam Voice 실패 시 Photon Voice/Vivox로 교체 가능하도록 인터페이스 분리 |
| **SoundPulseSystem** | 이벤트 생성 → 5.6 차폐 판정 → 5.7 배율 적용 → PerceivedPulse 송신 | 5.5 데이터 구조, 오브젝트 풀링(파문 프리팹 재사용) | 동시 파문 100개에서 60fps 목표(15.5절 리스크) |
| **RoleSystem** | 역할 배정·전이 | 3.3 상태 다이어그램 | 태그 판정(콜라이더 트리거)도 이 시스템이 소유 |
| **ItemSystem** | 아이템 스폰·상호작용·쿨다운 | 7절 아이템 표 | 리스폰 타이머 관리 |
| **ReplaySystem** | 사운드맵 타임랩스 기록 | 판별 SoundPulse 로그(세션 메모리만, 디스크 미저장) | 포스트 MVP |

### 15.4 GameFlowManager 상태표

| 상태 | 진입 동작 | 종료 조건 | 다음 상태 |
|---|---|---|---|
| Boot | 마이크 권한 확인 | 권한 처리 완료 | MainMenu |
| MainMenu | UI 노출 | 방 생성/입장 선택 | Lobby |
| Lobby | 방코드 발급, 준비 상태 수집 | 전원 Ready | RoleAssign |
| RoleAssign | 3초 연출, 역할 배정(RoleSystem 호출) | 연출 종료 | InGame |
| InGame | 맵 로드, 타이머 시작 | 6.3 승패 조건 충족 | RoundEnd |
| RoundEnd | 어워드 판정(8절), 결과 화면 노출 | 리매치 투표 종료 | Lobby(투표 부결) / RoleAssign(투표 가결, 즉시 재시작) |

### 15.5 핵심 기술 리스크 3가지와 검증 순서

| 순위 | 항목 | 목표 | 검증 시점 | 실패 시 대응 |
|---|---|---|---|---|
| 1 (최우선) | 파문 렌더 성능 + 리스너별 차폐 레이캐스트 | URP 스크린스페이스 셰이더(월드 거리장 근사) 또는 확장 링 메시 GPU 인스턴싱으로 동시 파문 100개 @ 60fps, 5.6 레이캐스트 포함 | 1~2주차 | 이게 안 되면 게임이 없다 — 즉시 대안 셰이더 검토 |
| 2 | Steam Voice 원시 PCM 접근 | 진폭·근접 감쇠·3D 패닝 구현, 5.2 대역 필터 포함 | 3~4주차 | Photon Voice/Vivox로 전환(진폭 스칼라+위치만 요구하므로 스택 종속 낮음) |
| 3 (포스트 MVP) | AI 관리인 청각 내비게이션 | 소리 기억 기반 수색 상태기계 | MVP 이후 | 실패해도 코어(PvP 술래)는 무손상 |

---

## 16. 아트 디렉션 스펙

### 16.1 스타일 정의

완전한 흑 배경 위에 발광 라인과 파문만으로 그리는 모노크롬 + 포인트 컬러. 텍스처·라이팅·고급 에셋 사용량 0. 파문의 감쇠 곡선과 라인 굵기 리듬이 곧 아트 퀄리티다.

### 16.2 컬러 팔레트

| 용도 | 기본 HEX | 색맹 모드 HEX |
|---|---|---|
| 배경 | `#000000` | 변경 없음 |
| 환경 파문 | `#E8E8E8` → 투명 감쇠 | 변경 없음 |
| 도망자(시안) | `#35F0D0` | `#F2E205` (옐로우) |
| 술래(레드) | `#FF3B4D` | `#3357FF` (블루) |
| 상호작용 오브젝트(앰버) | `#FFB84D` | `#C98A2E` (채도 낮춘 톤, 옐로우와 명확히 구분) |

색은 다섯 개면 충분하다. 신규 UI 요소도 이 팔레트 밖으로 나가지 않는다.

**형태적 구분(색맹 모드와 무관하게 상시 적용)**: 술래가 발생시킨 파문 링은 **이중 테두리**, 도망자/환경음 파문 링은 **단일 테두리**로 그려 색만이 아니라 형태로도 발생원을 구분할 수 있게 한다.

### 16.3 1인 제작 전략

- 지오메트리는 ProBuilder 블록아웃 수준으로 충분(플레이어에게는 라인만 보임)
- 소품은 실루엣 판독성 기준 20종 이내로 제한
- 파문 셰이더 1개를 전 오브젝트가 공유 — 아트 파이프라인 자체를 셰이더 하나로 수렴

---

## 17. 사운드 디렉션 스펙

| 구분 | 내용 |
|---|---|
| 원칙 | 사운드 = 렌더러. 모든 SFX는 시각 파문과 1:1 매핑, 들리지 않으면 보이지 않음 |
| 긴장 오디오 | 술래 8m 접근 시 본인 전용 페이드인 심장박동, 잠수 시 로우패스 먹먹함, 술래 전용 저역 드론 앰비언스 |
| 웃음 오디오 | 비명 = 화면 전체 개화 + 술래 화면 대형 표적 마커. 고무오리 삑삑, 메아리 노크 콩콩 리듬은 의도적으로 귀엽게 — 공포·코미디 온도차가 클수록 웃김 |
| 클립 생산 장치 | 판 종료 후 그 판의 모든 파문을 30초로 압축 재생하는 '사운드맵 타임랩스'(포스트 MVP) |

---

## 18. 콘텐츠 구성 (모드 · 뮤테이터, v1.0)

| 항목 | 내용 |
|---|---|
| 모드 — 클래식 술래 | 코어 규칙 그대로의 로테이션 |
| 모드 — 관리인 모드 | 2~3인 협동 vs AI 술래 '수영장 관리인'(소수 인원 진입로) |
| 뮤테이터 — 속삭임 판 | 고함 금지, 전 반경 반감 |
| 뮤테이터 — 라디오 판 | 맵에 켜진 라디오 3대가 소음 은신처 형성 |
| 뮤테이터 — 반전 판 | 술래만 발화 가능. "마르코?" 외치면 도망자 위치에서 자동 "폴로" 응답 파문 발생(원형 놀이의 직역) |

---

## 19. 접근성 설계

이 게임의 핵심 입력(발화)은 구조적으로 청각장애인에게는 문제가 없다 — 모든 파문이 시각화되기 때문이다. 다만 발화 자체가 어려운 사용자(언어장애, 소음 환경, 심리적 부담)에게는 진입 장벽이 된다. 아래를 확정 대응으로 명시한다.

| 대상 | 대응 | 스코프 |
|---|---|---|
| 마이크 미보유/사용 곤란 | 찰칵이(7절) + 솔로 연습장(12.2)으로 최소 참여 경로 제공 | MVP |
| 발화 자체가 어려운 사용자 | **텍스트 파문**: 채팅창에 문구 입력 시 "속삭임" 등급과 동일한 SoundPulse가 자신의 위치에서 발생. 등급을 속삭임으로 고정해 밸런스 훼손 방지 | v1.x |
| 색각 이상 | 16.2 색맹 모드 팔레트 + 파문 링 형태 구분 | MVP |
| 청각 정보 의존 접근성 보강 | 12.4 파문 자막 옵션("속삭임/대화/고함" 텍스트 라벨) | MVP |
| 실생활 인터럽트 | 5.8 강제 마이크 뮤트 키, PTT 전환 옵션 | MVP |

---

## 20. MVP 정의 & 합격 기준 (10주)

### 20.1 포함

- 수영장 그레이박스 1맵(10절)
- 4인(술래 1 + 도망자 3, 전원 사람)
- 밸브 3개 · 탈출 루프
- VoIP 시야 생성 + 소리 등급 3종(속삭임·대화·고함) + 발소리
- 5.6 사운드 차폐 규칙, 5.7 인지 배율, 5.8 VAD 파이프라인 전체
- 1인칭 조작 체계(4절) 및 기본 키 바인딩
- 찰칵이 1종
- 방코드 입장
- 결과 화면(어워드 1종)
- 설정 화면 4개 탭(12.6) 및 색맹 모드(16.2)

### 20.2 제외 (전부 후순위)

AI 술래, 폐극장 맵, 뮤테이터, 소리통·고무오리, 스킨, 사운드맵 타임랩스, 관전 고도화, 컨트롤러, 호스트 마이그레이션, 텍스트 파문(19절), 크라우치

### 20.3 합격 기준

친구 4인이 외부 디스코드 없이 3세트(9판)를 자발적으로 연속 플레이하고, 10판째에도 비명과 웃음이 발생하며, 술래 승률이 40~60% 구간에 들어올 것. 셋 중 하나라도 미달이면 콘텐츠 추가가 아니라 9절의 코어 수치부터 수정한다.

---

## 21. 개발 로드맵 (총 26주)

| 마일스톤 | 주차 | 산출물 | 확인 질문 |
|---|---|---|---|
| M1 기술 스파이크 | 1~2 | 파문 셰이더 + 로컬 마이크 진폭 데모(싱글) + 1인칭 컨트롤러 | "말하면 보인다" 하나만으로 "오" 소리가 나오는가 |
| M2 넷코드 스파이크 | 3~4 | FishNet + Steam 로비 2PC 연결, VoIP 공간화, 5.6 차폐 레이캐스트 프로토타입 | 친구 PC에서 내 목소리 파문이 0.3초 내에 뜨는가, 벽 뒤에서 감쇠가 체감되는가 |
| M3 MVP | 5~10 | 20절 정의 그대로 | 10판째에도 웃긴가, 술래 승률 40~60%인가 |
| M4 수직 완성 | 11~16 | 수영장 미감·주스, 아이템 3종, 어워드, 데모 빌드 | 테스트 영상에서 클립 3개가 자생적으로 나오는가 |
| M5 노출 | 17~20 | 스팀 페이지 + 데모 공개, Next Fest 참가 | 위시리스트 5,000을 향한 곡선이 그려지는가 |
| M6 출시 | 21~26 | 폐극장 맵, 관리인 모드(AI), 뮤테이터 3종, 폴리시 → EA 출시 | 출시 주 최고 동접 300+, 리뷰 "매우 긍정적" 유지가 가능한가 |

---

## 22. 스팀 출시 전략

| 항목 | 내용 |
|---|---|
| 가격 | ₩9,800, 출시 기념 -10%, 4인 팩 -15% |
| 데모 | 코어 1맵·연속 2판 제한, 상시 유지 |
| 프렌드 패스 | 구매자 1인당 게스트 슬롯 2개(EA 기간) |
| 클립 시딩 | 공포·합방 유튜버/버튜버 10팀에 4키 묶음 발송, '사운드맵 클립 내보내기'로 쇼츠 소재 직접 생산, 해시태그 #마르코게임, 시청자 참여형 방(방코드 공개) 공식 지원 |
| 위시리스트 목표 | 출시 시점 7,000(전환율 15~20% 가정 시 첫 주 1,100~1,400장), Next Fest 1회 참가(M5) |
| 출시 타이밍 | 10월 말 할로윈 시즌, AAA 대작 발매 주 회피 |
| 스토어 자산 | 캡슐 아트: "어둠 속 비명 파문 한가운데의 붉은 실루엣" 한 컷. 마이크 권장 사양 명시로 잘못된 기대 구매 차단 |

---

## 23. 리스크 톱 5와 대응책

| # | 리스크 | 대응책 |
|---|---|---|
| 1 | 마이크 장벽(무마이크 유저, 발화 수치심) | 찰칵이·솔로 연습장으로 최소 보조 제공하되 '마이크 게임' 포지셔닝 사수. 스토어에 명시해 기대치 정렬. 19절 텍스트 파문(v1.x)으로 장기 완화 |
| 2 | 4인 모으기 허들 | 프렌드 패스 + 관리인 모드(AI 술래) + 데모 유저 크로스세션 허용 검토 |
| 3 | VoIP 기술 삽질 | M2에 2주 타임박스. 실패 시 Photon Voice/Vivox 전환(진폭+위치만 요구해 전환 비용 낮음). 최악에도 푸시투토크(5.8 PTT 옵션)+로컬 진폭만으로 게임 성립 |
| 4 | 공포 강도 과다로 라이트 유저 이탈 | '파티 톤' 옵션(술래 스킨 괴물↔오리, 사운드 톤 스왑) — 공포는 연출이지 룰이 아니므로 전환 비용 낮음 |
| 5 | 카피 리스크(룰 단순=모방 용이) | 속도전. 데모 선공개로 원조 각인, 파문 아트·사운드 시그니처 일관 유지, 월 1회 뮤테이터 업데이트로 커뮤니티 선점 |

---

## 부록 A. 수치 레퍼런스 총정리

```
[이동속도]
  도망자 기본        5.0 m/s
  도망자 질주        7.5 m/s
  술래 기본          5.0 × (1.05~1.12) m/s  (인원별)
  메아리             8.0 m/s (콜라이더 없음)

[캐릭터/카메라]
  FOV                기본 90°, 조절 범위 75°~100°
  캐릭터 콜라이더 반경 0.35m (태그 판정 1.2m와 별개)
  시점                1인칭 확정, 크라우치 MVP 미포함

[소리 등급 — 반경 / 지속]
  속삭임    4m  / 0.6s   (RMS -50~-30 dBFS, 방향 게이지 트리거 안 됨)
  대화      9m  / 1.2s   (RMS -30~-15 dBFS, 방향 게이지 사거리 캡 13.5m)
  고함      22m / 2.5s   (RMS -15 dBFS↑, 방향 게이지 사거리 캡 33m)
  걷기      2m  / 0.4s
  질주      6m  / 0.8s
  밸브 회전 12m / 3s (6인 3.75s)
  노크(메아리) 9m / 1.2s  (30s 쿨다운)

[사운드 차폐]
  일반 벽 통과당 반경 배율   ×0.5 (중첩 가능)
  커튼(HardBlocker)         완전 차단(이벤트 미전송)
  지속시간 최소 유지 배율    ×0.5 (그 이하로는 안 깎임)

[역할별 인지 배율]
  술래  반경 ×1.2, 지속 ×1.5 (서버 판정 단계에 적용되는 기능적 수치)
  도망자/메아리  ×1.0

[재질 배율]
  타일        ×1.3
  나무 마루   ×1.2 (강제 발생)
  매트/카펫   ×0.7
  물(수면 아래) 파문 미발생, 숨 게이지 8s
    회복: 부상 2초 후 초당 +2(완전 회복 4s), 강제 부상 시 3초 -20% 이속 + 고함 강제 발생

[태그 판정]
  접촉 반경   1.2m, 1회 접촉 즉시 확정

[밸브/시간 — 인원별, MVP는 4인만 구현]
  2인: 밸브2 · 이속+5%  · 6분   (v1.x, AI 술래 필요)
  3인: 밸브2 · 이속+6%  · 8분   (v1.x)
  4인: 밸브3 · 이속+8%  · 10분  (✅ MVP)
  5인: 밸브3 · 이속+10% · 10분  (v1.x)
  6인: 밸브3(회전+25%) · 이속+12% · 12분  (v1.x)

[음성 입력]
  기본 방식        VAD(오픈마이크), PTT는 설정에서 대체 가능
  대역 필터        80Hz~3,400Hz
  연속성 최소 프레임 3프레임(60ms)
  디바운스         0.15초 미만 무시

[목표 지표]
  술래 승률 40~60%
  MVP 합격: 4인 3세트(9판) 자발적 연속 플레이, 10판째 웃음 발생
```

## 부록 B. 용어집 (Glossary)

| 용어 | 영문 | 정의 |
|---|---|---|
| 파문 | SoundPulse | 소리 발생 시 생성되는 시야 이벤트. 위치·반경·지속시간·타입 포함 |
| 리스너 | Seeker | 술래 역할, 세션당 1인(v1.x 대형맵 2인) |
| 도망자 | Runner | 밸브를 열고 탈출해야 하는 역할 |
| 메아리 | Echo | 태그당해 탈락한 도망자. 유령 상태로 노크 능력을 통해 계속 참여 |
| 밸브 | Valve | 회전시켜 여는 탈출 게이트 트리거 |
| 리매치 | Rematch | 라운드 종료 후 과반 투표로 즉시 역할 재추첨·재시작하는 것 |
| 뮤테이터 | Mutator | 같은 맵의 규칙 문법을 재조합하는 선택적 라운드 변형 |
| 사운드맵 타임랩스 | Soundmap Timelapse | 판 종료 후 해당 판의 모든 파문을 30초로 압축 재생하는 리플레이 기능 |
| 차폐 | Occlusion | 벽·지형지물에 의해 파문의 실제 인지 반경이 감쇠되거나 완전히 차단되는 현상(5.6) |
| 인지 배율 | Perception Multiplier | 리스너의 역할에 따라 동일한 물리적 파문을 다르게 감지하는 배율(5.7) |
