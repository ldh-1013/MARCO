# 마르코! (MARCO!) — 팀 역할 배분 및 협업 프로세스

> 작성 기준: Phase 4 진행 중 / 스프린트 27+ / 테스트 732케이스 통과 시점
> 대상: 기획 1 · 프로그래밍 4 · 디자인 1 (총 6인)
> 이 문서는 **팀 운영**만 다룬다. 게임 규칙·수치의 정본은 `docs/마르코_상세기획서.md`이며, 이 문서는 그것을 대체하지 않는다.

---

## 0. 이 문서의 목적

1인 개발로 Phase 3까지 완성된 코드베이스에 5명이 새로 합류한다. 이미 만들어진 구조(3계층 분리, 서버 권위 원칙, GAP 추적, 정본 문서 체계)를 **누가 어디까지 책임지는가**로 옮기는 것이 이 문서의 전부다.

핵심 전제 하나: **이 프로젝트는 코드보다 문서가 먼저다.** 수치와 규칙은 전부 기획서에서 오고, 코드는 그것을 옮긴 결과물이다. 구현하다 막혔다고 수치를 창작하면 나중에 반드시 어긋난다(이 프로젝트에서 이미 2회 발생).

---

## 1. 프로젝트 현황 요약 (신규 합류자용)

| 항목 | 값 |
|---|---|
| 장르 | 비대칭 호러 멀티플레이 (음성 술래잡기) |
| 핵심 루프 | 완전한 암전 → 소리 발생 → SoundPulse → 제한적 시야 → 위치 노출 → 추적/회피 |
| 정식 밸런스 | **4인 (술래 1 + 러너 3)**, 지원 2~6명 |
| 엔진/네트워크 | Unity 6.3 LTS / FishNet + Steamworks (테스트 AppID 480) |
| 아키텍처 | **Core / Net / Presentation 3계층 분리** (경계 위반 금지) |
| 씬 | `Lobby.unity`(시스템·상시 활성) + `Game.unity`(맵) 2개 동시 사용 |
| 테스트 | EditMode 732케이스 전수 통과 유지가 기본선 |
| 진행 단계 | Phase 4(품질/강화). 기획 재설계 → 코드 반영 6단계까지 완료, 실기 검증 대기 |

**한 줄 판단 기준**: "실제로 Steam에서 친구 3~4명이 한 판 플레이했을 때 '한 판 더 하자'가 나오는가?"

---

## 2. 팀 구성과 역할 배분

### 2.1 전체 배분표

| 역할 | 인원 | 한 줄 정의 |
|---|---|---|
| 기획 | 1 | 정본 소유자 · 수치 결정권자 · 최종 검수 |
| P1 — Core 게임 로직 | 1 | 밸브 · 승리조건 · 태그 · 역할 · 어워드 |
| P2 — 사운드/파문 시스템 | 1 | SoundPulse · 음성 · 노크 · 외침 · 숨 게이지 |
| P3 — 네트워크/인프라 | 1 | FishNet 동기화 · 서버 권위 · 빌드 · 성능 |
| P4 — Presentation/UX | 1 | 컨트롤러 · 카메라 · 입력 · HUD · 결과화면 |
| 디자인 | 1 | 아트 디렉션 전반 · 맵 미감 · UI 비주얼 · 스토어 자산 |

분업 축을 **계층(Core/Net/Presentation)** 이 아니라 **계층 + 시스템 혼합**으로 잡은 이유: 사운드/파문 계열이 이 게임의 유일한 핵심 훅이면서 Core·Net·Presentation 세 계층에 모두 걸쳐 있어, 계층으로만 나누면 가장 중요한 시스템에 오너가 없어진다.

---

### 2.2 기획 (1명)

| 구분 | 내용 |
|---|---|
| 소유 문서 | `마르코_상세기획서.md`(**유일 정본**), `프로젝트_현황_종합.md`, `수동검증_절차.md` |
| 결정 권한 | 모든 수치, 규칙, 승리조건, 밸런스, 시스템 추가/삭제 여부 |
| 상시 업무 | GAP 트리아지·우선순위 배정, 구현 결과 검수, 실기 플레이테스트 진행, 스프린트 계획 |
| 하지 않는 일 | 직접 구현(예외: 문서 수정) |

**검수가 이 역할의 본체다.** 구현자가 낸 결과를 "좋다"고 승인하는 게 아니라, 수치를 다시 계산하고 코드를 직접 열어 보고서와 대조한다. 실제로 이 방식으로 지금까지 지시 오류 1건(내가 반대로 쓴 조건식)과 치명 결함 1건(`ServerKnockDriver.Reset()` 호출부 부재 — 2라운드부터 노크 영구 봉인)을 잡았다.

---

### 2.3 P1 — Core 게임 로직

| 구분 | 내용 |
|---|---|
| 담당 시스템 | 밸브(§6.1), 승리조건(§6.3), 태그(§3.1), 역할 배정(§6.2), 어워드 3종(§8) |
| 주요 파일 | `Core/Objectives/*`(Valve, WinConditionEvaluator, ServerValveDriver), `Core/Role/*`(RoleAssigner, EchoVisibility), `Core/Tagging/*`, `Core/Awards/AwardTally.cs`, `Core/GameFlow/ServerRoundDriver.cs` |
| 난이도 | 중 — 순수 로직 위주라 EditMode 테스트로 전부 덮인다 |
| 주의 | 승리조건은 `RoundResult` enum을 통해 Presentation까지 파급된다. 변경 시 반드시 P4와 동시 작업 |

**당면 과제**: 1인 탈출 기믹(비상 배기구) 도입 시 승리조건 3분법 재작업이 이 역할의 첫 작업이 된다.

---

### 2.4 P2 — 사운드/파문 시스템

| 구분 | 내용 |
|---|---|
| 담당 시스템 | SoundPulse 판정·차폐·전파(§5.1/§5.6/§5.7), 음성 파이프라인 8단계(§5.2), 재질 배율(§5.9), 메아리 노크(§3.2), 술래 외침(§3.5), 숨 게이지(§5.9-1) |
| 주요 파일 | `Core/SoundPulse/*`, `Core/Sound/*`(KnockConfig, ServerKnockDriver, ServerShoutDriver, SeekerShoutConfig, FootstepMaterial), `Core/Voice/*`, `Core/Breath/*`, `Presentation/Voice/*`, `Presentation/SoundPulse/*` |
| 난이도 | **최상** — 이 게임에서 가장 복잡하고, 가장 자주 틀리고, 가장 중요하다 |
| 주의 | 용어 3분류(**발생 반경 / 청취 반경 / 방향 표시 반경**)를 절대 혼용하지 말 것. 섞이면 "방향 게이지가 청취 반경을 넘는" 모순이 생긴다(실제 발생했고 §3.4 정정으로 해결) |

**반드시 먼저 읽을 것**: 기획서 §5 전체 + §5.0-1(소음량 vs 노출 면적 — 두 지표를 통일하지 않는 이유).

---

### 2.5 P3 — 네트워크/인프라

| 구분 | 내용 |
|---|---|
| 담당 시스템 | FishNet 동기화 전반, 서버 권위 강제, 세션 흐름(§13), 이벤트 프로토콜(§14.3), 빌드·배포, 성능 프로파일링 |
| 주요 파일 | `Net/*` 전체(PulseNetworkSync, RoundNetworkSync, RoleNetworkSync, TagNetworkSync, ValveNetworkSync, ConnectionService), `Assets/_Project/Net/Transports/*` |
| 난이도 | 상 — FishNet 스폰 순서·소유권 타이밍 문제가 재현이 어렵다 |
| 주의 | **GAP-24 원칙 — 클라이언트 주장값 금지.** 클라이언트는 "무슨 일이 일어났는가"만 주장하고, 위치·반경·지속·역할·쿨다운은 전부 서버가 재계산한다. 새 RPC를 만들 때 이 원칙을 지켰는지 자문할 것 |

**이미 알려진 함정**: `LocalPlayerRegistry.Current`를 필드에 캐시하면 원격 pawn을 붙잡는다(GAP-61, 4곳에서 발생). 매 프레임 재조회가 정답이다.

---

### 2.6 P4 — Presentation/UX

| 구분 | 내용 |
|---|---|
| 담당 시스템 | 1인칭 컨트롤러, 카메라, 입력(§4.3), HUD(§12.4), 로비/결과/설정 화면(§12), 파문 렌더링 |
| 주요 파일 | `Presentation/Player/*`, `Presentation/UI/*`, `Presentation/SoundPulse/PulseVisualRenderer.cs`, `Presentation/GameFlow/*`, `Presentation/Echo/*` |
| 난이도 | 중상 — 로직은 단순하나 접점이 가장 많다 |
| 주의 | **디자이너와 유일하게 직접 맞물리는 역할.** 아트 자산을 실제로 씬·프리팹에 꽂는 사람이 이쪽이다 |

**당면 과제**: 키 리바인딩 시스템(§4.3) — 이것 하나로 GAP-42/44/50/64/65가 한꺼번에 해소된다. 현재 노크 키가 임시 F로 하드코딩돼 있다.

---

### 2.7 디자인 (1명)

| 구분 | 내용 |
|---|---|
| 담당 | 캐릭터 실루엣·림라이트, 맵 그레이박스 → 미감 패스, 파문 셰이더, UI 비주얼, 스토어 자산(캡슐 아트) |
| 소유 스펙 | 기획서 §16(아트 디렉션), §12(UI 와이어프레임)의 비주얼 부분 |
| 참고 자료 | `marco-imagegen-prompts.md` — AI 컨셉아트 생성용 프롬프트 모음 |
| 제약 | **색은 5개면 충분하다**(§16.2). 신규 UI 요소도 팔레트 밖으로 나가지 않는다. 새 색이 필요하면 기획서 §16.2에 먼저 추가한 뒤 사용 |

**작업 순서 권고**: 컨셉아트(현재 가능) → 캐릭터 저폴리 모델 1종(3역할이 같은 메시, 림라이트만 다름) → 맵 v2 그레이박스 미감 패스 → UI. 맵은 52×40m L자 2층 13구역으로 전면 교체 예정이라, 현재 그레이박스에 미감을 입히는 작업은 **하지 말 것**(버려진다).

---

## 3. 개인별 작업 배분 — 지금 당장 무엇을 하는가

> 아래는 **2026-09-12 시점의 실제 백로그를 6명에게 나눈 것**이다. 역할 정의(2절)가 "무엇을 책임지는가"라면, 이 절은 "이번 달에 무엇을 만드는가"다.
> 스프린트가 끝날 때마다 기획이 이 표를 갱신한다.

### 3.0 현재 대기 중인 작업 전량

| # | 작업 | 담당 | 선행 조건 | 규모 |
|---|---|---|---|---|
| 1 | **7-A 밸브 구조·역류** | P1 | 없음(즉시 착수) | 3~4일 |
| 2 | **7-B 승리조건·이벤트·태그** | P1 + P3 + P4 | 7-A 완료 | 3~4일 |
| 3 | **C6 `PhysicsOcclusionProbe.MaxHits = 32`** | P2 | 없음(**맵 v2 전 필수**) | 반나절 |
| 4 | **키 리바인딩(§4.3)** | P4 | 없음 | 2~3일 |
| 5 | **물 볼륨 감지** | P2 + P4 | 없음 | 2일 |
| 6 | **맵 v2 그레이박스** | 디자인 + 기획 | 밸브 5개 확정(완료) | 1~2주 |
| 7 | **바닥 재질 태그 배선** | P2 + 디자인 | 맵 v2 그레이박스 | 1일 |
| 8 | **§29 실기 검증** | 전원(최소 3인) | 7-A·7-B 완료 + exe 재빌드 | 반나절 ×N회 |
| 9 | **컨셉아트 → 캐릭터 모델** | 디자인 | 없음(즉시 착수) | 1~2주 |
| 10 | **카메라 bob ↔ 파문 동기화** | P4 | 없음 | 1일 |

### 3.1 기획 (마스터리)

| 우선순위 | 작업 | 산출물 |
|---|---|---|
| 1 | **탈출자 관전 규칙 결정** — 노크 가능 여부·음성 채널 소속 | 기획서 §13.3 갱신 |
| 2 | 7-A / 7-B 지시서 작성 및 검수 | Claude Code 프롬프트 + 검수 보고 |
| 3 | 맵 v2 밸브 배치 **30m 이격 검증**(§10.2-1) | 10쌍 실측표 |
| 4 | 실기 검증 진행 및 밸런스 판정 | GAP 등록 + 승률 측정 |
| 5 | 1인 탈출 기믹 도입 여부 결정 | 기획서 §6.3 재작업 여부 |

**막고 있는 것**: 1번(탈출자 관전 규칙)이 미정이라 7-A에 함께 넣을 수 없다. 먼저 결정할 것.

### 3.2 P1 — Core 게임 로직

| 순서 | 작업 | 파일 | 완료 기준 |
|---|---|---|---|
| 1 | `ValveState`에 `Reflowing` 추가 | `Core/Objectives/ValveState.cs` | 상태 전이 테스트 통과 |
| 2 | 밸브 D·E 회전시간 상수 | `Core/Objectives/Valve.cs` | 5개 상수 전부 §6.1 표와 일치 |
| 3 | **역류 타이머**(150초 → 30초 폐쇄) | `Core/Objectives/Valve.cs` | 경계값(150.0 / 180.0초) 테스트 |
| 4 | **5개 중 4개 랜덤 활성**(서버 권위) | `Core/Objectives/ServerValveDriver.cs` | 5가지 조합 전수 테스트 |
| 5 | 게이트 **동시 개방 수 판정 + latch** | `Core/Objectives/EscapeGateRegistry.cs` | 역류로 닫혀도 게이트 유지 검증 |
| 6 | `WinConditionEvaluator` → `gateOpened` | `Core/Objectives/WinConditionEvaluator.cs` | 3 Runner 전수검증 10/10 재확인 |

**주의**: 6번은 3단계에서 이미 한 번 고친 코드다. 당시 테스트(`TwoTagged_SeekerWinsImmediately` 등)의 **의미가 바뀌므로 이름부터 다시 봐야 한다.**

### 3.3 P2 — 사운드/파문 시스템

| 순서 | 작업 | 파일 | 완료 기준 |
|---|---|---|---|
| 1 | **C6 `MaxHits = 32` 수정** | `Presentation/SoundPulse/PhysicsOcclusionProbe.cs` | 구역 13개 + 2층 + 재질 6종에서 절단 없음 |
| 2 | 역류 **물소리 파문**(12m, 10초마다 ×3) | `Core/Sound/` + `ServerPulseDriver` | 새 `SoundType` 만들지 않고 Valve 등급 재사용 |
| 3 | **태그 파문**(고함급 22m ×1) | `ServerPulseDriver` 호출부 | `Scream` 집계에 섞이지 않는지 확인 |
| 4 | 물 볼륨 감지(P4와 공동) | `Core/Breath/BreathConfig.ZoneOf` 입력 | `isOnWaterSurface` 하드코딩 제거 |
| 5 | 바닥 재질 태그 배선 | `PhysicsFootstepMaterialProbe` + 씬 | 6종 재질이 실제로 다른 반경을 내는지 |

**1번이 가장 급하다.** 맵 v2는 구역 13개 + 2층 + 재질 6종이라 레이캐스트 히트가 32를 넘고, 그러면 **차폐 판정이 에러 없이 조용히 틀려진다.** 맵 작업보다 먼저 끝나 있어야 한다.

### 3.4 P3 — 네트워크/인프라

| 순서 | 작업 | 파일 | 완료 기준 |
|---|---|---|---|
| 1 | 신규 이벤트 4종 | `Net/ValveNetworkSync.cs` | `ValveActivated` / `ValveReflowing` / `ValveClosed` / `EscapeGateOpened` |
| 2 | 활성 밸브 조합 **서버 권위 전파** | `Net/ValveNetworkSync.cs` | 클라이언트가 조합을 주장할 수 없을 것 |
| 3 | 역류 타이머 동기화 | `Net/ValveNetworkSync.cs` | 지연 200ms에서 폐쇄 시점 오차 확인 |
| 4 | **exe 재빌드 파이프라인** | 빌드 설정 | 실기 검증 때마다 수동 반복하지 않도록 |
| 5 | 실기 검증 환경 구성 | — | 호스트 1 + exe 2, 3인 접속 확인 |

**4번을 먼저 해두면 8번 작업(실기 검증)의 반복 비용이 크게 준다.** 지금까지 스크립트가 바뀔 때마다 수동 재빌드를 해왔다.

### 3.5 P4 — Presentation/UX

| 순서 | 작업 | 파일 | 완료 기준 |
|---|---|---|---|
| 1 | **카메라 bob ↔ 파문 동기화** | `Presentation/Player/FirstPersonController.cs` | 발 닿는 순간 = 파문 = SFX, 셋이 동일 프레임 |
| 2 | **키 리바인딩(§4.3)** | `Presentation/UI/SettingsScreen.cs` | **GAP-42/44/50/64/65 동시 해소** |
| 3 | HUD **밸브 슬롯 4칸 + 역류 점멸** | `Presentation/UI/InGameHud.cs` | 동시 개방 수가 감소하는 것까지 표시 |
| 4 | **태그 연출**(플래시·FOV·경직 1.0초) | `FirstPersonController` + `TagDetector` | `연출.md` 4.2 타임라인 그대로 |
| 5 | 물 볼륨 감지(P2와 공동) | `FirstPersonController.cs:383` | 하드코딩 `false` 제거 |

**1번은 연출이 아니라 기능이다.** 발소리 타이밍을 예측할 수 없으면 은신 플레이가 실력이 아니라 운이 된다(`연출.md` §2.1).
**2번은 투자 대비 회수가 가장 크다** — GAP 5건이 한 번에 사라진다.

### 3.6 디자인

| 순서 | 작업 | 산출물 | 완료 기준 |
|---|---|---|---|
| 1 | 컨셉아트 생성 | 캐릭터 3종 · 맵 핵심 구역 | `marco-imagegen-prompts.md` 사용 |
| 2 | **캐릭터 저폴리 모델 1종** | `.fbx` + 머티리얼 | 3역할이 **같은 메시**, 림라이트만 다름 |
| 3 | **맵 v2 그레이박스**(기획과 공동) | ProBuilder 블록아웃 | 52×40m L자 2층 13구역 + 밸브 5개 |
| 4 | 애니메이션 클립 6종 | `연출.md` §12 목록 | 실루엣 판독 수준이면 충분 |
| 5 | 파문 셰이더 · UI 비주얼 | 셰이더 1개 공유 | §16.3 파이프라인 수렴 |

**하지 말 것**: 현재 그레이박스(45×35m 단층)에 미감 패스. **맵 v2로 전면 교체되므로 버려진다.**

---

## 4. 협업 매트릭스 — 누구와 무엇을 함께 하는가

### 4.1 상시 접점

| 접점 | 무엇을 공유하는가 | 누가 소유하는가 | 조율 규칙 |
|---|---|---|---|
| **P1 ↔ P3** | 밸브 상태·승리 판정의 네트워크 전파 | 판정 = P1(Core) / 전파 = P3(Net) | P1이 규칙을 확정한 뒤 P3가 전파를 붙인다. **순서를 뒤집지 않는다** |
| **P1 ↔ P4** | `RoundResult`·`ValveState` enum | enum = P1 | **enum 변경은 같은 커밋에서 양쪽 동시 처리.** P4에 알리지 않은 enum 변경은 결과화면을 조용히 깨뜨린다 |
| **P2 ↔ P3** | 파문의 서버 권위 전파 | 판정 = P2(Core) / 전파 = P3(Net) | `IPulseNetworkBridge` 변경은 **양쪽 합의 후에만** |
| **P2 ↔ P4** | 파문 시각화·방향 게이지 | 판정 = P2 / 표현 = P4 | 판정과 표현을 절대 섞지 않는다. P4는 P2가 준 결과를 그리기만 한다 |
| **P2 ↔ 디자인** | 바닥 재질 태그 | 태그 정의 = P2 / 씬 적용 = 디자인 | 디자이너가 구역을 만들 때 **재질 태그를 함께 붙인다.** 나중에 일괄로 붙이면 반드시 빠진다 |
| **P4 ↔ 디자인** | 아트 자산의 씬·프리팹 반영 | 에셋 = 디자인 / 배선 = P4 | 디자이너는 `.fbx`·머티리얼까지, 씬에 꽂는 것은 P4 |
| **디자인 ↔ 기획** | 맵 레이아웃 | 구역 규격·밸브 배치 = 기획 / 블록아웃 = 디자인 | 기획이 치수를 주고 디자인이 만든 뒤, **기획이 30m 이격을 실측 검증** |
| **전원 ↔ 기획** | 모든 수치 | 기획서 = 기획 | 코드에 수치를 창작하지 않는다. 기획서에 없으면 **GAP으로 기록하고 물어본다** |

### 4.2 공동 제작이 필요한 작업 (혼자 못 끝내는 것)

| 작업 | 참여자 | 왜 함께 해야 하는가 | 진행 방식 |
|---|---|---|---|
| **7-B 승리조건·이벤트·태그** | P1 + P3 + P4 | 판정(P1) → 전파(P3) → 표시(P4)가 한 줄로 이어져 있어 따로 만들면 접합부에서 깨진다 | P1이 먼저 규칙 확정 → P3·P4 동시 착수 → 셋이 함께 통합 테스트 |
| **물 볼륨 감지** | P2 + P4 | 물 판정(Core 규칙)과 콜라이더 감지(Presentation)가 한 몸 | P2가 `ZoneOf` 입력 계약을 정하고, P4가 그 값을 채운다 |
| **맵 v2 그레이박스** | 디자인 + 기획 (+P2) | 치수·밸브 배치는 밸런스 사안, 블록아웃은 아트, 재질 태그는 사운드 | 기획 치수 → 디자인 블록아웃 → 기획 실측 검증 → P2 재질 태그 |
| **실기 검증** | **전원(최소 3인)** | 혼자서는 태그 2명 도달조차 재현할 수 없다 | 주 1회 고정 시간, 호스트 1 + exe 2 |
| **재질 태그 배선** | P2 + 디자인 | 태그 정의는 코드, 부착은 씬 | 디자이너가 구역 제작 시 동시에 부착 |

### 4.3 절대 혼자 결정하면 안 되는 것

| 항목 | 반드시 합의할 상대 | 이유 |
|---|---|---|
| `enum` 값 추가·변경 | 그 enum을 읽는 전원 | 컴파일은 되는데 런타임에 조용히 틀려진다 |
| 인터페이스 시그니처 변경 | 구현체·호출부 소유자 | 계층 경계가 무너진다 |
| 기획서 수치 | **기획** | 코드가 정본이 되는 순간 문서 체계가 붕괴 |
| 씬·프리팹 구조 변경 | 6절 편집권 규칙에 따라 전원 공지 | 머지 불가능 |
| 새 `SoundType` 추가 | **기획** (원칙적으로 금지, §3.3) | 5.1 등급 체계 전체에 영향 |

---

## 5. 회의 구조

> **회의는 네 종류면 충분하다.** 이보다 늘리지 않는다 — 6명짜리 팀에서 회의가 늘면 그게 곧 개발 시간 손실이다.

| 회의 | 주기 | 참석 | 시간 | 목적 |
|---|---|---|---|---|
| **스프린트 킥오프** | 주 1회 | 전원 | 30분 | 이번 주 작업 배분, 선행 조건 확인, 블로커 공유 |
| **실기 플레이테스트** | 주 1회 | **최소 3인** | 60분 | §29 체크리스트 실행 + 버그 발견 |
| **인터페이스 합의** | 필요시 | 관련자만 | 15분 | enum·인터페이스 변경 전 5분 대화. **이걸 건너뛰면 하루를 잃는다** |
| **아트 리뷰** | 격주 | 디자인 + P4 + 기획 | 30분 | 컨셉·모델·UI 방향 확인, 팔레트 준수 점검 |

### 5.1 스프린트 킥오프에서 반드시 확인할 것

| 항목 | 확인 내용 |
|---|---|
| 선행 조건 | 이번 주 작업 중 다른 사람 완료를 기다리는 것이 있는가 |
| 씬·프리팹 | 이번 주에 씬/프리팹을 건드릴 사람이 있는가 (**충돌 예방**) |
| enum·인터페이스 | 변경 예정이 있는가, 있다면 누구와 합의해야 하는가 |
| GAP | 지난주 등록된 GAP 중 이번 주에 처리할 것 |

### 5.2 실기 플레이테스트 진행 순서

| 단계 | 내용 |
|---|---|
| 0 | **exe 재빌드 확인**(스크립트·프리팹 변경 시 필수) |
| 1 | 스모크 — 이동·발소리·태그·밸브·탈출 |
| 2 | `수동검증_절차.md` §29 체크리스트 |
| 3 | 발견 즉시 **GAP 번호 부여** → `phase-4-build-log.md` 기록 |
| 4 | 막힘/일반/사소 분류 → 다음 스프린트 배분 |

**플레이테스트는 버그 찾기이자 재미 검증이다.** "한 판 더 하자"가 나오는지를 매번 기록할 것 — 그게 이 프로젝트의 최종 합격 기준이다.

### 5.3 회의가 필요 없는 것

| 상황 | 대신 |
|---|---|
| 구현 방법 질문 | 기획서 해당 절 먼저 읽기 → 없으면 GAP 등록 |
| 진행 상황 공유 | `phase-4-build-log.md` 기록으로 대체 |
| 수치 확인 | 기획서 부록 A 수치 레퍼런스 |

---

## 6. 공동 작업 규칙

### 6.1 씬·프리팹 편집권 (가장 먼저 사고가 나는 지점)

> Unity 씬(`Lobby.unity` · `Game.unity`)과 `Player.prefab`은 **텍스트 머지가 사실상 불가능하다.** 두 사람이 동시에 건드리면 한쪽 작업이 통째로 사라진다.

| 규칙 | 내용 |
|---|---|
| **선점 공지** | 씬·프리팹을 여는 순간 팀 채널에 "`Game.unity` 잡습니다" 공지 |
| **해제 공지** | 커밋·푸시 후 "놨습니다" 공지 |
| **동시 편집 금지** | 공지된 파일은 다른 사람이 열지 않는다 |
| **장시간 점유 금지** | 반나절 이상 잡아야 하면 킥오프에서 미리 알린다 |
| **충돌 시** | 머지하지 말 것. **나중에 잡은 쪽이 자기 작업을 다시 한다** |

### 6.2 커밋 규칙

| 규칙 | 내용 |
|---|---|
| enum·인터페이스 변경 | **영향받는 전체를 같은 커밋에** 넣는다 |
| 테스트 | 코드와 **같은 커밋**에. 나중에 붙이면 안 붙는다 |
| 문서 | 구현이 기획서와 달라졌으면 **그 커밋에서 기획서도 갱신**(기획 승인 후) |
| 빌드 깨진 상태 | 푸시 금지. 732케이스 통과가 기본선 |

### 6.3 GAP 등록은 누구나, 판단은 기획

| 단계 | 담당 |
|---|---|
| 기획서에 정의가 없다는 것을 발견 | **누구나** |
| GAP 번호 부여 + 기록 | 발견자 |
| 해석·결정 | **기획만** |
| 임시로 코드에 값을 넣는 것 | **금지.** 막히면 물어본다 |

---

## 7. 작업 프로세스

### 7.1 스프린트 운영

현재 스프린트 번호(27+)를 그대로 이어간다. 1스프린트 = 1~5일 규모의 명확한 단위 작업.

```
① 기획이 정본에 규칙·수치 확정 반영
② 담당 오너가 구현 (+ EditMode 테스트 동시 작성)
③ 기획이 검수 — 코드 직접 대조, 수치 재계산
④ 통과 시 커밋 · phase-4-build-log.md에 기록
⑤ 실기 검증 (누적해서 일괄 진행)
```

### 7.2 구현 → 검수 절차

구현자는 완료 보고 시 **반드시** 아래를 포함한다. 형식이 지켜지지 않으면 검수가 불가능하다.

| 항목 | 내용 |
|---|---|
| 변경 전/후 | 값·조건식·시그니처를 나란히 |
| 계산 검증표 | "~할 것이다"가 아니라 실제 수치 계산. 경계값 포함 |
| 테스트 | 신규·갱신 케이스명과 통과 여부 |
| 전체 재검증 | 전 어셈블리 오류/경고, 테스트 총계 변화 |
| 범위 밖으로 남긴 것 | 발견했지만 이번에 안 고친 것 |

**문제가 하나라도 남으면 PASS라고 보고하지 않는다.**

**지시와 기존 기록(빌드로그·GAP 목록·기획서)이 충돌하면 실행하지 말고 먼저 알린다.** 실제로 이 규칙 덕에 기획의 지시 오류를 구현자가 잡아낸 사례가 있다.

### 7.3 GAP 관리

기획서에 정의가 없거나 모순이 있는 지점은 **창작하지 않고 GAP으로 기록**한다. 현재 GAP-1~68까지 추적 중.

| 상태 | 의미 |
|---|---|
| ✅ 해소 | 기획서 보완 또는 구현으로 종결 |
| ⏭ 이월 | 선행 시스템 부재로 뒤 단계에서 처리 |
| 📌 결정 유지 | 의도적으로 현 상태를 유지하기로 결정 |

기록 위치: `phase-4-build-log.md`. 총계는 `프로젝트_현황_종합.md` §4-A 전수 상태표에 반영.

### 7.4 실기 검증

| 단계 | 내용 |
|---|---|
| 0 | exe 재빌드(스크립트·프리팹 변경 시 필수). **최소 3인**(호스트 1 + exe 2) — 2인은 태그 2명 도달이 불가능해 절반 이상 검증 불가 |
| 1 | 스모크: 이동·발소리·태그·밸브·탈출 |
| 2 | 신규 기능(`수동검증_절차.md` §29 체크리스트) |
| 3 | 버그 트리아지 → GAP 번호 부여 → 우선순위 분류 |
| 4 | 수정 후 재검증 |
| 5 | 4인 확장 테스트(정식 밸런스 기준) |

---

## 8. 문서 관리 원칙

> **정본은 하나만 둔다. 정본이 둘이면 반드시 어긋난다 (이 프로젝트에서 이미 2회 발생).**

| 문서 | 지위 |
|---|---|
| `마르코_상세기획서.md` | **유일한 정본.** 절 번호(§X)가 코드베이스 전체의 참조 주소 |
| `프로젝트_현황_종합.md` | 진행 상태·GAP 전수표 |
| `phase-4-build-log.md` | 스프린트별 구현 기록·판단 근거 |
| `수동검증_절차.md` | 실기 검증 체크리스트 |
| `팀_역할배분.md` | 이 문서 — 팀 운영 |
| `marco-imagegen-prompts.md` | 컨셉아트 생성용 프롬프트 |

**절 번호(§X)는 수백 회 인용되는 참조 주소다. 임의 재배치 금지.**

---

## 9. 코드 규칙 (계층 경계)

| 계층 | 허용 | 금지 |
|---|---|---|
| **Core** | 순수 C# 로직, 규칙, 상태기계 | `UnityEngine.Time`·`Input`·`Physics` 참조, `MonoBehaviour`, FishNet 참조 |
| **Net** | FishNet, 서버 권위 판정, 동기화 | 게임 규칙을 여기서 새로 정의하는 것 |
| **Presentation** | MonoBehaviour, 렌더링, 입력, UI | 판정 로직을 여기서 수행하는 것 |

**Core가 순수해야 EditMode 테스트가 성립한다.** 이 프로젝트가 732케이스를 유지할 수 있는 유일한 이유다.

추가 규칙:
- 새 상수를 만들기 전에 기존 상수 재사용 가능성을 먼저 확인한다(값이 두 곳으로 갈라지면 반드시 어긋난다)
- 유도 가능한 값은 상수로 저장하지 않고 계산으로 유도한다 (예: `MaxConsecutiveSuppressions => (int)(TotalSeconds / SuppressionCost)`)
- 기획서 원문을 XML 주석에 인용하고, 기획서가 갱신되면 주석도 함께 고친다

---

## 10. 결정이 필요한 사항 (기획 판단 대기)

| # | 항목 | 선택지 |
|---|---|---|
| 1 | **Claude Code 사용 정책** | (a) 전원 사용 (b) 기획만 오케스트레이션, 프로그래머는 직접 구현 (c) 혼용 |
| 2 | **브랜치 전략** | 현재 단일 브랜치. 4명 병렬 작업 시 기능 브랜치 + PR 리뷰 게이트 필요 |
| 3 | **씬/프리팹 편집권 규칙 채택** | 6.1절에 권장안(선점 공지 → 해제 공지 → 충돌 시 나중에 잡은 쪽이 재작업)을 써 두었다. **채택 여부와 공지 채널만 정하면 된다** |
| 4 | **1인 탈출 기믹 도입 여부** | 도입 시 승리조건 3분법 재작업(P1 첫 과제)이 실기 검증보다 앞서야 함 |
| 5 | **탈출자 관전 규칙** | 탈출한 도망자가 노크를 쓸 수 있는가, 음성은 어느 채널인가. **미정이라 7-A 착수를 막고 있다** |

3번은 규칙을 안 정하면 **반드시 사고가 난다.** 프로그래머 4명이 동시에 씬을 건드리는 순간 하루치 작업이 사라진다.
5번은 코드 작업의 선행 조건이므로 가장 먼저 결정해야 한다.

---

## 11. 신규 합류자 온보딩 체크리스트

| 순서 | 항목 | 대상 |
|---|---|---|
| 1 | 기획서 §1~§3 (개요·역할·핵심 루프) 정독 | 전원 |
| 2 | 기획서 §5 (사운드 시스템) 정독 | 전원 — 이 게임의 정체성이다 |
| 3 | **이 문서 3절에서 자기 작업 목록 확인** | 전원 |
| 4 | **이 문서 4.1~4.3 + 6절(공동 작업 규칙) 숙지** | 전원 — 특히 6.1 씬 편집권 |
| 5 | 담당 시스템의 기획서 절 정독 | 각 오너 |
| 6 | `phase-4-build-log.md`에서 담당 시스템 관련 스프린트 기록 훑기 | 각 오너 |
| 7 | 로컬 빌드 + 3인 실기 1판 플레이 | 전원 — **문서만 읽고 시작하지 말 것** |
| 8 | EditMode 테스트 전체 실행해 732 통과 확인 | 프로그래머 |
| 9 | GAP 목록에서 담당 시스템 이월 항목 확인 | 각 오너 |
| 10 | `연출.md` 정독 | P4 · 디자인 |

7번을 건너뛰면 안 된다. "말해야 앞이 보이는데, 말하면 술래가 듣는다"는 문장은 한 판 해보기 전에는 이해되지 않는다.

**첫 주 목표**: 1~9번 완료 + 3절의 자기 항목 중 첫 번째 작업 착수.
