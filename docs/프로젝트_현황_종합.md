# 마르코! (MARCO!) — 프로젝트 현황 종합

> 작성일: 2026-07-19
> 기준 문서: `docs/마르코_상세기획서.md`, `docs/phase-1-분석.md`, `docs/phase-2-foundation.md`, `docs/phase-3-build-log.md`
> 이 문서는 세 개의 개별 산출물 문서를 하나로 엮은 **진행 상황 스냅샷**이다. 상세 근거·QA 기록은 각 원본 문서에 있고, 여기서는 "지금까지 뭘 했고 뭐가 남았는지"만 훑을 수 있게 정리한다.

---

## 0. 한눈에 보는 진행률

| Phase | 상태 | 핵심 산출물 |
|---|---|---|
| Phase 1 — 전략/분석 | ✅ 완료 | GAP 7건 식별, GAP-1~3 결정 확정, M3 태스크 분해(T1~T25) |
| Phase 2 — 기초/스캐폴딩 | ✅ 완료 | 어셈블리 3분할, FishNet Unity 6.5 호환 패치, 씬 4개, 컬러 팔레트 |
| Phase 3 — 빌드(진행 중) | 🔶 진행 중 | 코어 루프 골격 4/4, 사운드 코어 3/4(배선 포함), **플레이 가능 + 발소리 파이프라인 가동** |

**테스트 누계: 102케이스 전수 통과, 컴파일 에러 0** (Unity 에디터 없이 `dotnet build` + 리플렉션 러너로 검증. 에디터 내 Test Runner 재확인은 아직 미실시).

**가장 중요한 이정표**: 이 세션에서 프로젝트가 **처음으로 "걸어다닐 수 있는" 상태**가 됐다(그레이박스 맵 + 1인칭 컨트롤러).

---

## 1. Phase 1 — 전략 및 아키텍처 분석 (완료)

`docs/phase-1-분석.md` 전문. 기획서(`마르코_상세기획서.md`) 자체가 이미 Phase 1 산출물 상당수(§9 밸런스 근거, §14~15 아키텍처, §20 MVP 정의, §21 로드맵, §23 리스크)를 포함하고 있어, 처음부터 다시 만들지 않고 **갭 지적 + 검증**에 집중했다.

### 1.1 스펙 갭 7건

| # | 내용 | 심각도 | 상태 |
|---|---|---|---|
| GAP-1 | 본인 파문에 역할 배율이 적용되는가 | 심각 | **결정**: 타인 발생 펄스에만 적용. 본인 파문은 배율 없이 로컬 렌더 |
| GAP-2 | PerceivedPulse가 정확한 좌표를 그대로 보냄 | 심각 | **결정**: 벽 0개(완전 시야)일 때만 좌표 공개, 그 외 8방위만 |
| GAP-3 | 차폐 판정이 1회인가 재계산인가 | 심각 | **결정**: 최초 1회 + 0.25초 간격 재판정 |
| GAP-4 | 방향 게이지와 차폐의 관계 미명시 | 보통 | 권장안 제시(§5.6 통과 펄스만 게이지 트리거) — 구현에 자연 반영됨 |
| GAP-5 | 메아리가 밸브를 돌릴 수 있는가 | 보통 | **결정**: 불가 → T3 구현에 반영 완료 |
| GAP-6 | §5.2 캘리브레이션이 MVP 목록 누락 | 보통 | 권장안 제시(미착수 기능) |
| GAP-7 | M3 6주에 설정 화면 전체가 과적재 | 보통 | 권장안 제시(M3 필수 3탭 / M4 이월) — 아직 미착수 |

### 1.2 아키텍처 검증 결과

- **Core / Net / Presentation 3분할** 타당성 확인, `SoundPulseSystem`을 `Core.SoundPulseResolver`(순수 판정) + `Presentation.PulseRenderer`(렌더링)로 분리 권고 → **Phase 2에서 그대로 구현됨**
- 기술 리스크 재평가: 동시 파문 목표를 100개→30개로 하향 조정 권고

### 1.3 M3(MVP) 태스크 분해 — 6주 순서

5주차(코어 루프 골격) T1~T4, 6주차(사운드 코어) T5~T8이 정의됨. 이후 7주차 이후(T9~T25, 음성 파이프라인·UI·네트워크 동기화 등)는 원 문서에 개요만 있고 세부 미전개.

---

## 2. Phase 2 — 기초 및 스캐폴딩 (완료)

`docs/phase-2-foundation.md` 전문.

### 2.1 패키지 및 의존성

| 패키지 | 상태 |
|---|---|
| Facepunch.Steamworks 2.3.6 | OpenUPM 레지스트리로 설치 |
| ProBuilder 6.1.2 | Unity 공식 레지스트리 (실제로는 그레이박스에 미사용, 큐브 프리미티브로 대체) |
| **FishNet** | git 패키지 방식 **포기** → `Assets/FishNet/`에 소스 벤더링 후 직접 패치 |

### 2.2 FishNet Unity 6000.5 호환성 패치 (핵심 작업)

Unity 6000.5.4f1에서 FishNet 4.7.2가 그대로 컴파일되지 않음 — 두 가지 Unity 6.5 API 폐기가 원인.

1. `Object.GetInstanceID()` → `EntityId` 전환, 부호 검사 관용구 대체 불가
2. `Scene.handle`의 `int` 암시적 변환 제거 → `SceneHandle` 구조체로 전환

**조치**: 씬 핸들 저장 타입을 `int`→`ulong`(raw data)로 8개 파일에 걸쳐 전면 수정, `NetworkObserver.cs`는 `#if UNITY_6000_5_OR_NEWER` 분기 처리. FishNet 공식 저장소에는 아직 이 패치가 없음 — 이 프로젝트가 자체 대응한 것.

**검증**: Unity 에디터 없이 `dotnet build`로 csproj 12개 전부 컴파일 에러 0 확인, 리플렉션 러너로 Core.Tests 실행해 통과 확인.

### 2.3 어셈블리 구조 확립 (§15.2)

```
Assets/_Project/
  Core/            (FishNet 모름 — 순수 로직)
  Net/             (Core + FishNet.Runtime)
  Presentation/    (Core [+ 이번 세션에 Unity.InputSystem 추가])
  Tests/EditMode/  (Core + nunit)
```

### 2.4 완료된 핵심 산출물

- **SoundPulseResolver**: GAP-1~3 결정을 그대로 구현한 순수 함수. `IOcclusionProbe` 인터페이스로 Physics 주입 분리 → EditMode 테스트 가능
- **GameFlowManager**: §15.4 7개 상태 전이를 전이표로 강제하는 상태기계
- **씬 4개**: Boot → MainMenu → Lobby → Game(구 SampleScene 리네임), Build Settings 등록
- **ColorPalette**: §16.2 5색 + 색맹 모드, ScriptableObject
- **NetworkBridge**: FishNet NetworkManager 격리 계층 골격(RPC 배선은 비어있음)

### 2.5 의도적으로 하지 않은 것 (Phase 2 시점)

- Steam AppID는 테스트용 480(Spacewar) 고정
- RPC/SyncVar 배선 없음
- UI 없음(빈 씬)
- 파문 셰이더 없음

---

## 3. Phase 3 — 빌드 및 반복 (진행 중)

`docs/phase-3-build-log.md` 전문 + 이번 세션 추가 작업.

### 3.1 스프린트 1 — 코어 루프 골격 (5주차, 완료)

| # | 태스크 | 상태 | 테스트 |
|---|---|---|---|
| T1 | GameFlowManager 상태기계 | ✅ (Phase 2 선완료) | 9케이스 |
| T2 | 맵 그레이박스 (§10.1 8구역) | ✅ | 구조 검증 |
| T3 | 밸브 상태기계 (§6.1) | ✅ | 17케이스 |
| T4 | 승패 판정 (§6.3) | ✅ | 11케이스 |

**T2 특기사항**: 원래 "ProBuilder는 에디터 밖에서 못 만든다"고 블로킹 보고했으나, **큐브 프리미티브 방식으로 우회 완료**. Game.unity에 `Graybox` 루트 아래 59개 오브젝트로 로비·라커룸A/B·샤워장·메인 풀 홀·기계실·라이프가드실·보일러 통로·배수로 출구·술래 격리실을 구현. 벽 49개 전부 `Wall` 태그 + `SoundBlocking` 레이어 배선 완료(TagManager에 태그·레이어 신규 등록). §5.6 차폐 판정이 실제로 동작할 수 있는 물리적 기반이 마련됨. 의도적 단순화 3건(풀 홀 오픈 데크·라커룸B 비인접·유리창 생략)은 미감 단계로 이월.

### 3.2 스프린트 1 추가 — 사운드 코어 일부 (6주차, 진행 중)

| # | 태스크 | 상태 | 비고 |
|---|---|---|---|
| T5 | SoundPulseResolver + 테스트 | ✅ | Phase 2에서 완료 |
| T6 | 차폐 재판정 주기 실측 | ⏳ 미착수 | 에디터 프로파일러 필요 |
| T7 | PerceivedPulse 재판정 오케스트레이션(GAP-3) | ✅ | `ActivePulseTracker`, 12케이스 |
| T8 | 파문 렌더러(셰이더) | ⏳ 미착수 | 에디터 필요 |

**T7 상세**: GAP-3("최초 1회 + 0.25초 간격 재판정, 변화분만 방출")을 순수 클래스로 구현. Appeared/Updated/Disappeared 3종 델리버리, 역할별 지속시간(술래 ×1.5) 반영, 재판정 미도래 시 레이캐스트 자체를 생략해 예산 보호.

### 3.3 스프린트 2 — §4 플레이어 이동 시스템 (완료, 이번 세션)

M3 태스크 분해에 없던 항목이지만 §21 로드맵상 M1 산출물("1인칭 컨트롤러")로, **이걸 구현하면서 프로젝트가 처음으로 플레이 가능 상태가 됨**.

| 계층 | 파일 | 역할 |
|---|---|---|
| Core | `Locomotion/MovementState.cs` | Idle/Walk/Sprint/Diving (§4.2) |
| Core | `Locomotion/LocomotionConfig.cs` | 속도 상수(러너 5.0/7.5, 술래 5.4, 메아리 8.0) + §5.1 발소리 수치 |
| Core | `Locomotion/LocomotionSimulator.cs` | 순수 로직: 입력→상태·속도·발소리 펄스 타이밍 |
| Presentation | `Player/FirstPersonController.cs` | Input System 폴링, CharacterController.Move, 마우스룩, 커서락 |

**주목할 스펙 해석**: §5.1 발소리 등급이 속도 기준(">5.0m/s")이라, **술래(5.4m/s)는 걷기만 해도 질주 등급(6m) 펄스**가 난다 — 문자 그대로 구현한 결과이며 러너에게 술래 접근 경보로 작용. 밸런스 영향은 플레이테스트 검증 항목으로 기록.

Game.unity에 `Player`(CharacterController 반경 0.35 + FirstPersonController, RunnerSpawn1 위치) + `PlayerCamera`(FOV 90) 배치, 기존 Main Camera는 비활성화.

**테스트**: LocomotionSimulator 18케이스 추가(역할별 속도 4종·Sprint 제한·상태 전이·대각 정규화·펄스 발생/주기·Diving 상태) — **테스트 총계 68 → 86**.

**아직 안 된 것**: 수면 존 감지 미배선(잠수 진입 불가). 밸브 상호작용 UI 없음. (~~발소리 이벤트 미구독~~ → 스프린트 3에서 해소)

### 3.4 스프린트 3 — 발소리 펄스 → 게임플레이 파이프라인 배선 (완료)

발소리 발생(스프린트 2)과 펄스 판정(T5/T7)을 잇는 순수 배선. 새 게임플레이 규칙 없음, Core 무수정, Net 참조 추가 없음.

| 파일 (전부 Presentation/SoundPulse/) | 역할 |
|---|---|
| `PhysicsOcclusionProbe.cs` | §5.6 차폐 판정의 **첫 Unity Physics 구현체**. Physics 호출과 순수 판정부(`Classify`) 분리 |
| `LocalPulsePipeline.cs` | 발소리 이벤트 → SoundPulse 변환 → AddPulse → Tick 구동 → 델리버리 방출(순수, 주입식) |
| `LocalPulsePipelineBehaviour.cs` | Unity 어댑터: 이벤트 구독·Time.time 틱·`[Pulse]` Console 로그(T8 전까지 시각 연출 없음) |

`Game.unity`에 `PulseSystem` 오브젝트 배치(FirstPersonController 참조 배선). 청취 기준점은 **스폰 위치 고정 가상 청취자(id=999) — 임시 스모크 리그이며 실제 게임플레이 기능 아님**(GAP-1 때문에 발생원과 다른 ID 필수). 테스트 10케이스 추가 — 총계 86 → 96.

### 3.5 버그 조사 — "Disappeared가 한 번도 안 뜬다" (조사 완료, 코드 수정 없음)

에디터 Play 검증 중 발견된 증상(펄스 ID가 건너뛰는 것처럼 보이고 Disappeared 로그가 전혀 없음)을 조사. **결론: 버그 아님.** 두 가지가 겹친 것으로 확인:
1. `ActivePulseTracker`의 "자연 만료는 통지 없음"은 T7부터 있던 의도적 설계(기존 테스트로 이미 검증됨)
2. 스프린트 3의 디버그 청취자가 스폰 위치에 고정돼 있어, 플레이어가 발소리 반경 밖으로 나가면 이후 펄스는 애초에 Appeared 자체가 안 남 — ID 스킵처럼 보인 원인

재현 테스트 6케이스로 실증(전부 첫 실행에 통과, 별도 수정 불필요) — **총계 96 → 102**. 상세: `docs/phase-3-build-log.md` "버그 조사" 절.

---

## 4. 전체 파일 인벤토리 (2026-07-19 기준)

### Core (`Assets/_Project/Core/`)
```
GameFlow/       GameFlowState.cs, GameFlowManager.cs
Locomotion/     MovementState.cs, LocomotionConfig.cs, LocomotionSimulator.cs
Objectives/     ValveState.cs, Valve.cs, RoundResult.cs, WinConditionEvaluator.cs
Role/           RoleType.cs
SoundPulse/     SoundType.cs, DirectionOctant.cs, SoundPulse.cs, PerceivedPulse.cs,
                IOcclusionProbe.cs, SoundPulseResolver.cs, ActivePulseTracker.cs
```

### Net (`Assets/_Project/Net/`)
```
NetworkBridge.cs
Transports/FishyFacepunch/   (8개 소스 파일, 원본 GUID 유지)
```

### Presentation (`Assets/_Project/Presentation/`)
```
Palette/     ColorPalette.cs
Player/      FirstPersonController.cs
SoundPulse/  PhysicsOcclusionProbe.cs, LocalPulsePipeline.cs, LocalPulsePipelineBehaviour.cs
```

### Tests (`Assets/_Project/Tests/EditMode/`)
```
SoundPulseResolverTests.cs      19케이스
GameFlowManagerTests.cs          9케이스
ValveTests.cs                   17케이스
WinConditionEvaluatorTests.cs   11케이스
ActivePulseTrackerTests.cs      12케이스
LocomotionSimulatorTests.cs     18케이스
LocalPulsePipelineTests.cs       10케이스
LocalPulsePipelineExpiryTests.cs  6케이스
─────────────────────────────────────
합계                             102케이스, 전수 통과
```

### 씬
```
Boot.unity / MainMenu.unity / Lobby.unity   — 카메라만 있는 빈 스켈레톤
Game.unity                                   — Graybox(59오브젝트) + Player + PlayerCamera + PulseSystem
```

### 벤더링된 외부 소스
```
Assets/FishNet/          FishNet 4.7.2 전체(Unity 6.5 패치 적용)
```

---

## 5. 앞으로 할 작업

### 5.1 다음 작업 후보

1. **T8 파문 렌더러**: 스프린트 3이 만든 `LocalPulsePipeline.DeliveryEmitted`를 Debug.Log 대신 실제 파문 링/방향 게이지 시각화로 교체 — 배선이 끝났으므로 소비자만 갈아끼우면 됨
2. **T6 재판정 주기 실측**: 그레이박스+파이프라인이 가동 중이므로 에디터 프로파일러로 0.25s 재판정 비용 측정 가능
3. **에디터 Play 검증**: 스프린트 3 체크리스트(§빌드로그) — SoundBlocking 레이어 첫 실물 검증 포함

### 5.2 6주차 잔여

- **T6**: 차폐 재판정 0.25초 주기의 실제 프레임타임 영향 실측(6인 기준 <1ms 목표) — Unity 에디터 프로파일러 필요
- **T8**: 파문 렌더러(셰이더 + 동시 30개 @60fps) — 에디터 필요, GAP-2의 SourcePos/DirectionOctant 분기 렌더링 포함

### 5.3 7주차 이후 (미착수, 세부 미전개)

기획서 §20.1 MVP 포함 목록 기준:
- **음성 파이프라인**(§5.2/§5.8): VAD, 대역 필터, 연속성 검사, 개인 캘리브레이션, 3등급(속삭임/대화/고함) 실시간 분류
- **역할 배정 시스템**: RoleType 배정 로직(현재 FirstPersonController에 인스펙터 고정값만 있음), 러너 이탈 시 메아리 전환(§13.3)
- **네트워크 동기화**: FishNet RPC/SyncVar 배선(§14.3 이벤트 테이블 전체) — NetworkBridge는 골격만 있음
- **UI**: 로비(방코드), 메인메뉴, 결과 화면, 설정 4탭(§12.6, GAP-7 권고대로 M3 필수 3개만: 오디오/색맹/마우스 감도)
- **찰칵이(카메라 상호작용)**: 기획서 §7 아이템 시스템 관련, 미착수
- **밸브 E 상호작용 UI/입력 배선**: `Valve` Core 로직은 있으나 플레이어 입력과 연결 안 됨
- **VoIP**: 실제 음성 스트리밍(현재는 로컬 이동/발소리 펄스만 구현)

### 5.4 Unity 에디터에서 재확인 필요 (누적)

1. Game.unity 로드 확인, Graybox·Player 정상 표시
2. Test Runner에서 86케이스 재확인(현재는 스크래치패드 dotnet build 검증만 실시)
3. WASD/마우스/Shift 실제 플레이 테스트, FOV 90·커서 락 확인
4. Steamworks 초기화(Steam 클라이언트 필요)
5. Burst `Unity.Settings.Editor` 오류 소거 여부

---

## 6. 검증 방법론 메모 (재사용 가능한 프로세스)

이 프로젝트는 Unity 에디터에 실시간 연결하지 않고 파일 조작 + 오프라인 컴파일 검증으로 진행했다. 핵심 장치:

- **스크래치패드 `CoreVerify.csproj` / `PresentationVerify.csproj`**: Unity가 생성하는 `Core.csproj` 등은 소스 파일을 명시적으로 나열하고 에디터가 재임포트하기 전까지 갱신되지 않는다(1회 "헛빌드" 사고 발생 후 발견). 이를 우회하기 위해 `Assets/_Project/**/*.cs`를 glob으로 잡는 별도 csproj를 만들어, Unity 관리 DLL(`C:\Program Files\Unity\Hub\Editor\6000.5.4f1\...`)을 그대로 참조해 컴파일한다.
- **리플렉션 기반 테스트 러너**: `[Test]`/`[TestCase]` 속성을 리플렉션으로 찾아 실행하는 자체 러너로 nunit.framework.dll만 있으면 Unity Test Framework 없이도 테스트를 돌릴 수 있다.
- **meta 파일 수기 생성**: 배경 자동 생성기가 불완전한 meta(1줄짜리)를 만들거나 GUID를 중복시키는 문제를 여러 차례 발견 → 매 작업 후 GUID 중복 검사 스크립트로 전수 검증하는 루틴 확립.
- **씬 YAML 직접 조작**: ProBuilder 없이 큐브 프리미티브로 그레이박스를 만들 때, fileID 중복·참조 무결성·SceneRoots 등록을 매번 파이썬 스크립트로 검증.

이 방법론 덕분에 Unity 에디터가 실제로 열리기 전에 컴파일 에러와 로직 결함 대부분을 잡아낼 수 있었지만, **에디터 전용 동작(렌더링·입력 실제 반응·프로파일링)은 여전히 사람이 직접 열어서 확인해야 한다.**
